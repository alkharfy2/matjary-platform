# دليل تنفيذ المرحلة 0 (P0) — أساسيات الإطلاق

> **تاريخ الإنشاء**: 25 مارس 2026  
> **المرجعية**: خطة-المميزات-الجديدة.md  
> **الهدف**: دليل تنفيذ تفصيلي لكل ميزة من مميزات P0 الثمانية  
> **المستوى**: جاهز للتنفيذ (Copy-Paste Ready)

---

## الفهرس

1. [Facebook Pixel + Conversion API](#1-facebook-pixel--conversion-api)
2. [TikTok Pixel](#2-tiktok-pixel)
3. [Google Analytics 4](#3-google-analytics-4)
4. [WhatsApp Integration](#4-whatsapp-integration)
5. [Transactional Emails (Resend)](#5-transactional-emails-resend)
6. [تصدير الطلبات CSV/Excel](#6-تصدير-الطلبات-csvexcel)
7. [طباعة الفواتير وبوليصات الشحن](#7-طباعة-الفواتير-وبوليصات-الشحن)
8. [إشعارات الطلبات Real-time](#8-إشعارات-الطلبات-real-time)

---

## نظرة عامة على التغييرات المشتركة

### تغييرات قاعدة البيانات (stores.settings)

كل مميزات الـ Tracking (Pixel, TikTok, GA, WhatsApp) تُحفظ في عمود `settings` (JSONB) الموجود فعلاً في جدول `stores`.

**النوع الحالي لـ StoreSettings**:
```typescript
{
  currency: 'EGP' | 'SAR' | 'AED' | 'USD'
  language: 'ar' | 'en'
  direction: 'rtl' | 'ltr'
  showOutOfStock: boolean
  requirePhone: boolean
  requireEmail: boolean
  minOrderAmount: number | null
  maxOrderAmount: number | null
  enableCod: boolean
  enableKashier: boolean
  kashierMerchantId: string | null
}
```

**النوع الجديد بعد التعديل**:
```typescript
{
  // === الحقول الموجودة (بدون تغيير) ===
  currency: 'EGP' | 'SAR' | 'AED' | 'USD'
  language: 'ar' | 'en'
  direction: 'rtl' | 'ltr'
  showOutOfStock: boolean
  requirePhone: boolean
  requireEmail: boolean
  minOrderAmount: number | null
  maxOrderAmount: number | null
  enableCod: boolean
  enableKashier: boolean
  kashierMerchantId: string | null

  // === حقول جديدة — Tracking Pixels ===
  facebookPixelId: string | null          // مثال: "123456789012345"
  facebookConversionApiToken: string | null // Access Token من Events Manager
  facebookTestEventCode: string | null    // مثال: "TEST12345" (للاختبار فقط)
  tiktokPixelId: string | null            // مثال: "CP1234567890"
  googleAnalyticsId: string | null        // مثال: "G-XXXXXXXXXX"
  snapchatPixelId: string | null          // مثال: "abc123-def456" (P1 لكن نجهز الحقل)

  // === حقول جديدة — WhatsApp ===
  whatsappFloatingEnabled: boolean        // default: true
  whatsappFloatingPosition: 'left' | 'right' // default: 'left' (RTL = يسار الشاشة)
  whatsappDefaultMessage: string | null   // رسالة افتراضية للزر العائم
  whatsappOrderButtonEnabled: boolean     // default: false (زر "اطلب عبر واتساب" في المنتج)

  // === حقول جديدة — Email Notifications ===
  emailNotificationsEnabled: boolean      // default: true
  merchantEmailOnNewOrder: boolean        // default: true
}
```

### الملفات المتأثرة بتغيير StoreSettings

| الملف | التغيير المطلوب |
|-------|----------------|
| `src/db/schema.ts` | تحديث نوع StoreSettings + default values |
| `src/lib/validations/store.ts` | إضافة حقول الـ settings الجديدة في `updateStoreSettingsSchema` |
| `src/app/api/dashboard/settings/route.ts` | السماح بتحديث الحقول الجديدة |
| `src/lib/tenant/store-context.tsx` | StoreContextValue يشمل settings كاملة بالفعل ✅ |
| `src/app/store/layout.tsx` | حقن سكربتات الـ Tracking (Pixel, TikTok, GA) |

### ملف Migrations

```sql
-- migrations/add_tracking_and_notifications.sql
-- لا يحتاج migration لأن settings هو JSONB
-- الحقول الجديدة تتضاف تلقائياً مع default values في الكود
-- Drizzle ORM بيتعامل مع JSONB كـ object وبيعمل merge
```

---

## 1. Facebook Pixel + Conversion API

### 1.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | كود JavaScript من Meta يتتبع زيارات وأحداث العملاء في المتجر، ويبعت البيانات لحساب Facebook Ads بتاع التاجر |
| **ليه مهم** | بدونه التاجر مش هيقدر يعمل إعلانات مستهدفة، ولا يقيس نتائج حملاته، ولا يعمل Custom Audiences |
| **المكونات** | 1) Client-side Pixel (JavaScript في المتصفح) 2) Server-side Conversion API (من السيرفر مباشرة لـ Meta) |
| **بدون حزم إضافية** | Pixel = script tag عادي. Conversion API = HTTP POST بـ `fetch()` — لا نحتاج SDK |

### 1.2 كيف يعمل Facebook Pixel

```
المتصفح                         Meta Servers
   │                                  │
   │ 1. صفحة تتحمّل                   │
   │ ──→ fbq('init', PIXEL_ID)        │
   │ ──→ fbq('track', 'PageView')  ──→│ يسجل الزيارة
   │                                  │
   │ 2. عميل يفتح منتج               │
   │ ──→ fbq('track', 'ViewContent')──→│ يسجل مشاهدة المنتج
   │                                  │
   │ 3. يضيف للسلة                    │
   │ ──→ fbq('track', 'AddToCart') ──→│ يسجل إضافة للسلة
   │                                  │
   │ 4. يبدأ checkout                 │
   │ ──→ fbq('track', 'InitiateCheckout')→│
   │                                  │
   │ 5. يكمل الطلب                    │
   │ ──→ fbq('track', 'Purchase')  ──→│ يسجل عملية شراء
```

### 1.3 كيف يعمل Conversion API (Server-Side)

```
السيرفر (Next.js API)              Meta Graph API
   │                                  │
   │  POST https://graph.facebook.com/v21.0/{PIXEL_ID}/events
   │  Headers: { Authorization: Bearer ACCESS_TOKEN }
   │  Body: {
   │    data: [{
   │      event_name: "Purchase",
   │      event_time: unix_timestamp,
   │      event_id: "unique_id",        ← لمنع التكرار مع Pixel
   │      action_source: "website",
   │      event_source_url: "https://store.matjary.com/checkout",
   │      user_data: {
   │        em: [sha256(email)],         ← hashed
   │        ph: [sha256(phone)],         ← hashed
   │        client_ip_address: "...",
   │        client_user_agent: "...",
   │        fbc: "fb.1.123.abc",         ← من الكوكي _fbc
   │        fbp: "fb.1.123.xyz"          ← من الكوكي _fbp
   │      },
   │      custom_data: {
   │        currency: "EGP",
   │        value: 350.00,
   │        content_ids: ["product-id-1"],
   │        content_type: "product"
   │      }
   │    }],
   │    test_event_code: "TEST12345"     ← فقط في الاختبار
   │  }
   │ ──────────────────────────────────→│ يسجل الحدث server-side
```

**لماذا Conversion API بالإضافة للـ Pixel؟**
- iOS 14+ وأنظمة الخصوصية بتحجب الـ client-side pixel
- الـ Conversion API بيبعت من السيرفر مباشرة — مش متأثر بأي حاجب
- Meta بيعمل deduplication تلقائي عبر `event_id` المشترك بين الاتنين
- النتيجة: تحسين الـ Event Match Quality (EMQ) وأداء الإعلانات

### 1.4 الملفات المطلوب إنشاؤها

```
src/
├── lib/
│   └── tracking/
│       ├── facebook-pixel.ts          # Client-side pixel utility functions
│       ├── facebook-capi.ts           # Server-side Conversion API
│       ├── tiktok-pixel.ts            # TikTok pixel utilities (ميزة 2)
│       ├── google-analytics.ts        # GA4 utilities (ميزة 3)
│       └── types.ts                   # أنواع مشتركة لكل الـ tracking
├── components/
│   └── tracking/
│       ├── facebook-pixel-script.tsx   # Script component (client)
│       ├── tiktok-pixel-script.tsx     # TikTok script (ميزة 2)
│       ├── google-analytics-script.tsx # GA4 script (ميزة 3)
│       └── tracking-scripts.tsx        # مجمّع — يحقن كل السكربتات معاً
└── app/
    └── store/
        └── layout.tsx                  # يُعدّل ليشمل <TrackingScripts />
```

### 1.5 التنفيذ خطوة بخطوة

#### الخطوة 1: أنواع الـ Tracking المشتركة

**ملف**: `src/lib/tracking/types.ts`

```typescript
/** معرّفات الـ Tracking لكل متجر — تُقرأ من store.settings */
export type TrackingConfig = {
  facebookPixelId: string | null
  facebookConversionApiToken: string | null
  facebookTestEventCode: string | null
  tiktokPixelId: string | null
  googleAnalyticsId: string | null
  snapchatPixelId: string | null
}

/** بيانات حدث e-commerce موحدة */
export type EcommerceEventData = {
  contentIds?: string[]
  contentName?: string
  contentCategory?: string
  contentType?: 'product' | 'product_group'
  currency?: string
  value?: number
  numItems?: number
  searchString?: string
  orderId?: string
  items?: Array<{
    id: string
    name: string
    price: number
    quantity: number
    category?: string
  }>
}

/** بيانات المستخدم للـ Conversion API (server-side) */
export type ConversionUserData = {
  email?: string         // سيتم هاشه بـ SHA-256
  phone?: string         // سيتم هاشه بـ SHA-256
  clientIpAddress?: string
  clientUserAgent?: string
  fbc?: string           // Facebook click cookie
  fbp?: string           // Facebook browser cookie
}
```

#### الخطوة 2: Facebook Pixel Client-Side

**ملف**: `src/lib/tracking/facebook-pixel.ts`

```typescript
/**
 * Facebook Pixel — Client-Side Tracking
 * 
 * يُستدعى فقط في client components أو في <Script> tags.
 * يستخدم fbq() global function اللي بيحملها الـ base code.
 * 
 * المرجع: https://developers.facebook.com/docs/meta-pixel/reference
 */

/** التحقق إن fbq موجود في window */
function getFbq(): ((...args: unknown[]) => void) | null {
  if (typeof window !== 'undefined' && typeof window.fbq === 'function') {
    return window.fbq
  }
  return null
}

/** تتبع حدث PageView — يتفعل تلقائياً في الـ base code */
export function trackPageView() {
  getFbq()?.('track', 'PageView')
}

/** تتبع مشاهدة منتج */
export function trackViewContent(data: {
  contentId: string
  contentName: string
  contentCategory?: string
  value: number
  currency: string
}) {
  getFbq()?.('track', 'ViewContent', {
    content_ids: [data.contentId],
    content_name: data.contentName,
    content_category: data.contentCategory,
    content_type: 'product',
    value: data.value,
    currency: data.currency,
  })
}

/** تتبع إضافة للسلة */
export function trackAddToCart(data: {
  contentId: string
  contentName: string
  value: number
  currency: string
  quantity?: number
}) {
  getFbq()?.('track', 'AddToCart', {
    content_ids: [data.contentId],
    content_name: data.contentName,
    content_type: 'product',
    value: data.value,
    currency: data.currency,
    ...(data.quantity && { num_items: data.quantity }),
  })
}

/** تتبع بدء Checkout */
export function trackInitiateCheckout(data: {
  contentIds: string[]
  value: number
  currency: string
  numItems: number
}) {
  getFbq()?.('track', 'InitiateCheckout', {
    content_ids: data.contentIds,
    content_type: 'product',
    value: data.value,
    currency: data.currency,
    num_items: data.numItems,
  })
}

/** تتبع شراء ناجح */
export function trackPurchase(data: {
  contentIds: string[]
  value: number
  currency: string
  numItems: number
  orderId?: string
}) {
  getFbq()?.('track', 'Purchase', {
    content_ids: data.contentIds,
    content_type: 'product',
    value: data.value,
    currency: data.currency,
    num_items: data.numItems,
    ...(data.orderId && { order_id: data.orderId }),
  })
}

/** تتبع بحث */
export function trackSearch(searchString: string) {
  getFbq()?.('track', 'Search', {
    search_string: searchString,
  })
}
```

#### الخطوة 3: Facebook Conversion API (Server-Side)

**ملف**: `src/lib/tracking/facebook-capi.ts`

```typescript
/**
 * Facebook Conversion API — Server-Side Events
 * 
 * يُستدعى من API Routes (checkout webhook, order creation, إلخ).
 * لا يحتاج أي حزمة إضافية — يستخدم fetch() مباشرة.
 * 
 * المرجع: https://developers.facebook.com/docs/marketing-api/conversions-api/using-the-api
 * Graph API Version: v21.0
 */

import { createHash } from 'crypto'

const GRAPH_API_VERSION = 'v21.0'
const GRAPH_API_BASE = 'https://graph.facebook.com'

/** هاش SHA-256 — مطلوب لبيانات المستخدم */
function sha256(value: string): string {
  return createHash('sha256')
    .update(value.trim().toLowerCase())
    .digest('hex')
}

type CAPIEventName =
  | 'PageView'
  | 'ViewContent'
  | 'AddToCart'
  | 'InitiateCheckout'
  | 'Purchase'
  | 'Search'

type CAPIUserData = {
  email?: string
  phone?: string
  clientIpAddress?: string
  clientUserAgent?: string
  fbc?: string
  fbp?: string
}

type CAPICustomData = {
  currency?: string
  value?: number
  contentIds?: string[]
  contentType?: 'product' | 'product_group'
  contentName?: string
  contentCategory?: string
  numItems?: number
  orderId?: string
  searchString?: string
}

type CAPIConfig = {
  pixelId: string
  accessToken: string
  testEventCode?: string | null
}

/**
 * إرسال حدث واحد إلى Facebook Conversion API
 * 
 * @param config - Pixel ID + Access Token + Test Event Code
 * @param event - اسم الحدث
 * @param userData - بيانات المستخدم (يتم هاشها تلقائياً)
 * @param customData - بيانات الحدث (منتجات، مبلغ، إلخ)
 * @param eventSourceUrl - رابط الصفحة اللي حصل فيها الحدث
 * @param eventId - معرّف فريد لمنع التكرار (deduplication مع Pixel)
 */
export async function sendConversionEvent(
  config: CAPIConfig,
  event: CAPIEventName,
  userData: CAPIUserData,
  customData: CAPICustomData,
  eventSourceUrl: string,
  eventId?: string,
): Promise<{ success: boolean; error?: string }> {
  try {
    const url = `${GRAPH_API_BASE}/${GRAPH_API_VERSION}/${config.pixelId}/events`

    // بناء user_data مع الهاش
    const hashedUserData: Record<string, unknown> = {}
    if (userData.email) hashedUserData.em = [sha256(userData.email)]
    if (userData.phone) hashedUserData.ph = [sha256(userData.phone)]
    if (userData.clientIpAddress) hashedUserData.client_ip_address = userData.clientIpAddress
    if (userData.clientUserAgent) hashedUserData.client_user_agent = userData.clientUserAgent
    if (userData.fbc) hashedUserData.fbc = userData.fbc
    if (userData.fbp) hashedUserData.fbp = userData.fbp

    // بناء custom_data
    const custom: Record<string, unknown> = {}
    if (customData.currency) custom.currency = customData.currency
    if (customData.value !== undefined) custom.value = customData.value
    if (customData.contentIds) custom.content_ids = customData.contentIds
    if (customData.contentType) custom.content_type = customData.contentType
    if (customData.contentName) custom.content_name = customData.contentName
    if (customData.contentCategory) custom.content_category = customData.contentCategory
    if (customData.numItems) custom.num_items = customData.numItems
    if (customData.orderId) custom.order_id = customData.orderId
    if (customData.searchString) custom.search_string = customData.searchString

    const body: Record<string, unknown> = {
      data: [
        {
          event_name: event,
          event_time: Math.floor(Date.now() / 1000),
          action_source: 'website',
          event_source_url: eventSourceUrl,
          user_data: hashedUserData,
          custom_data: custom,
          ...(eventId && { event_id: eventId }),
        },
      ],
    }

    // إضافة test_event_code لو موجود (للاختبار فقط)
    if (config.testEventCode) {
      body.test_event_code = config.testEventCode
    }

    const response = await fetch(`${url}?access_token=${config.accessToken}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    })

    if (!response.ok) {
      const errorData = await response.json().catch(() => ({}))
      console.error('[CAPI] Error:', JSON.stringify(errorData))
      return { success: false, error: `HTTP ${response.status}` }
    }

    return { success: true }
  } catch (error) {
    console.error('[CAPI] Exception:', error)
    return { success: false, error: String(error) }
  }
}
```

#### الخطوة 4: مكون Script في الـ Storefront

**ملف**: `src/components/tracking/facebook-pixel-script.tsx`

```typescript
'use client'

/**
 * Facebook Pixel Base Code Script
 * 
 * يُحقن في store/layout.tsx لو التاجر حاطط Pixel ID.
 * يُحمّل الـ fbevents.js ويعمل init + أول PageView.
 */

import { useEffect } from 'react'
import Script from 'next/script'

type FacebookPixelScriptProps = {
  pixelId: string
}

export function FacebookPixelScript({ pixelId }: FacebookPixelScriptProps) {
  useEffect(() => {
    // لما الصفحة تتغير (client-side navigation)، نبعت PageView
    if (typeof window !== 'undefined' && typeof window.fbq === 'function') {
      window.fbq('track', 'PageView')
    }
  }, [])

  return (
    <>
      <Script
        id="facebook-pixel"
        strategy="afterInteractive"
        dangerouslySetInnerHTML={{
          __html: `
            !function(f,b,e,v,n,t,s)
            {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
            n.callMethod.apply(n,arguments):n.queue.push(arguments)};
            if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
            n.queue=[];t=b.createElement(e);t.async=!0;
            t.src=v;s=b.getElementsByTagName(e)[0];
            s.parentNode.insertBefore(t,s)}(window, document,'script',
            'https://connect.facebook.net/en_US/fbevents.js');
            fbq('init', '${pixelId}');
            fbq('track', 'PageView');
          `,
        }}
      />
      <noscript>
        <img
          height="1"
          width="1"
          style={{ display: 'none' }}
          src={`https://www.facebook.com/tr?id=${pixelId}&ev=PageView&noscript=1`}
          alt=""
        />
      </noscript>
    </>
  )
}
```

#### الخطوة 5: دمج Conversion API في Checkout

**الملف المتأثر**: `src/app/api/checkout/route.ts`

**المكان**: بعد إنشاء الطلب بنجاح (بعد الـ transaction) — إضافة:

```typescript
// === بعد إنشاء الطلب بنجاح ===

// إرسال Purchase event إلى Facebook Conversion API (fire-and-forget)
if (store.settings?.facebookPixelId && store.settings?.facebookConversionApiToken) {
  sendConversionEvent(
    {
      pixelId: store.settings.facebookPixelId,
      accessToken: store.settings.facebookConversionApiToken,
      testEventCode: store.settings.facebookTestEventCode,
    },
    'Purchase',
    {
      email: validatedData.customerEmail ?? undefined,
      phone: validatedData.customerPhone,
      clientIpAddress: request.headers.get('x-forwarded-for') ?? undefined,
      clientUserAgent: request.headers.get('user-agent') ?? undefined,
    },
    {
      currency: store.settings?.currency ?? 'EGP',
      value: Number(total),
      contentIds: orderItems.map(item => item.productId),
      contentType: 'product',
      numItems: orderItems.length,
      orderId: orderNumber,
    },
    `${request.headers.get('origin') ?? ''}/checkout`,
    `${orderNumber}-${Date.now()}`,  // event_id for deduplication
  ).catch(() => {}) // fire-and-forget — لا نعطل الـ checkout لو فشل
}
```

### 1.6 إعدادات الداشبورد

**الملف المتأثر**: صفحة Settings في الداشبورد

تحتاج **قسم جديد** "إعدادات التتبع والإعلانات" في صفحة الإعدادات:

```
📊 إعدادات التتبع والإعلانات
├── Facebook Pixel ID          [________________]  (اختياري)
├── Conversion API Token       [________________]  (اختياري)
├── Test Event Code            [________________]  (للاختبار فقط)
├── TikTok Pixel ID            [________________]  (اختياري)
├── Google Analytics ID        [________________]  (اختياري)
└── [حفظ]
```

**Validation Schema الجديد** (يُضاف في `updateStoreSettingsSchema`):

```typescript
// يُضاف للحقول الموجودة في updateStoreSettingsSchema
facebookPixelId: z.string().max(20).trim().nullable().optional(),
facebookConversionApiToken: z.string().max(500).trim().nullable().optional(),
facebookTestEventCode: z.string().max(20).trim().nullable().optional(),
tiktokPixelId: z.string().max(30).trim().nullable().optional(),
googleAnalyticsId: z.string().max(20).trim().nullable().optional(),
snapchatPixelId: z.string().max(50).trim().nullable().optional(),
```

### 1.7 أماكن إطلاق أحداث Pixel في الـ Storefront

| الحدث | الملف | المكان بالتحديد |
|-------|-------|-----------------|
| `PageView` | `facebook-pixel-script.tsx` | تلقائي عند تحميل الصفحة + useEffect عند التنقل |
| `ViewContent` | `src/app/store/product/[slug]/page.tsx` | مكون client يستدعي `trackViewContent()` في useEffect |
| `AddToCart` | `src/app/store/product/[slug]/_components/product-details.tsx` | في handler إضافة السلة |
| `InitiateCheckout` | `src/app/store/checkout/page.tsx` | useEffect عند فتح صفحة الـ checkout |
| `Purchase` | `src/app/store/order-success/page.tsx` | useEffect عند تحميل صفحة النجاح |
| `Search` | `src/app/api/storefront/search/route.ts` | أو في مكون البحث client-side |

### 1.8 ملاحظات أمنية

- **الـ Pixel ID** آمن أنه يكون في الـ client (هو مُصمّم لده)
- **الـ Conversion API Token** يجب أن يبقى **في السيرفر فقط** — لا يتم إرساله للـ client أبداً
- **بيانات المستخدم** يتم هاشها بـ SHA-256 قبل الإرسال (مطلوب من Meta)
- الـ Conversion API call هو **fire-and-forget** — لو فشل لا يأثر على الـ checkout

---

## 2. TikTok Pixel

### 2.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | كود JavaScript من TikTok يتتبع أحداث العملاء لقياس أداء إعلانات TikTok |
| **ليه مهم** | TikTok أصبح من أكبر منصات الإعلانات في مصر، خصوصاً للشباب ومنتجات الـ dropshipping |
| **بدون حزم** | Script tag عادي — لا نحتاج SDK |

### 2.2 كيف يعمل

```
المتصفح                         TikTok Servers
   │                                  │
   │ 1. تحميل الصفحة                  │
   │ ──→ ttq.load(PIXEL_ID)           │
   │ ──→ ttq.page()               ──→│ PageView
   │                                  │
   │ 2. مشاهدة منتج                  │
   │ ──→ ttq.track('ViewContent') ──→│
   │                                  │
   │ 3. إضافة للسلة                  │
   │ ──→ ttq.track('AddToCart')   ──→│
   │                                  │
   │ 4. بدء checkout                 │
   │ ──→ ttq.track('InitiateCheckout')→│
   │                                  │
   │ 5. شراء ناجح                    │
   │ ──→ ttq.track('CompletePayment')→│
```

### 2.3 الملفات المطلوبة

**ملف**: `src/lib/tracking/tiktok-pixel.ts`

```typescript
/**
 * TikTok Pixel — Client-Side Tracking
 * 
 * المرجع: https://business-api.tiktok.com/portal/docs?id=1739585696931842
 */

function getTtq(): Record<string, (...args: unknown[]) => void> | null {
  if (typeof window !== 'undefined' && window.ttq) {
    return window.ttq as Record<string, (...args: unknown[]) => void>
  }
  return null
}

export function ttqTrackViewContent(data: {
  contentId: string
  contentName: string
  contentCategory?: string
  price: number
  currency: string
  quantity?: number
}) {
  getTtq()?.track?.('ViewContent', {
    contents: [{
      content_id: data.contentId,
      content_name: data.contentName,
      content_category: data.contentCategory,
      quantity: data.quantity ?? 1,
      price: data.price,
    }],
    content_type: 'product',
    value: data.price * (data.quantity ?? 1),
    currency: data.currency,
  })
}

export function ttqTrackAddToCart(data: {
  contentId: string
  contentName: string
  price: number
  currency: string
  quantity?: number
}) {
  getTtq()?.track?.('AddToCart', {
    contents: [{
      content_id: data.contentId,
      content_name: data.contentName,
      quantity: data.quantity ?? 1,
      price: data.price,
    }],
    content_type: 'product',
    value: data.price * (data.quantity ?? 1),
    currency: data.currency,
  })
}

export function ttqTrackInitiateCheckout(data: {
  contentIds: string[]
  value: number
  currency: string
}) {
  getTtq()?.track?.('InitiateCheckout', {
    contents: data.contentIds.map(id => ({ content_id: id })),
    content_type: 'product',
    value: data.value,
    currency: data.currency,
  })
}

export function ttqTrackCompletePayment(data: {
  contentIds: string[]
  value: number
  currency: string
  orderId?: string
}) {
  getTtq()?.track?.('CompletePayment', {
    contents: data.contentIds.map(id => ({ content_id: id })),
    content_type: 'product',
    value: data.value,
    currency: data.currency,
  })
}
```

**ملف**: `src/components/tracking/tiktok-pixel-script.tsx`

```typescript
'use client'

import Script from 'next/script'

type TikTokPixelScriptProps = {
  pixelId: string
}

export function TikTokPixelScript({ pixelId }: TikTokPixelScriptProps) {
  return (
    <Script
      id="tiktok-pixel"
      strategy="afterInteractive"
      dangerouslySetInnerHTML={{
        __html: `
          !function (w, d, t) {
            w.TiktokAnalyticsObject=t;var ttq=w[t]=w[t]||[];
            ttq.methods=["page","track","identify","instances","debug","on","off","once","ready","alias","group","enableCookie","disableCookie","holdConsent","revokeConsent","grantConsent"];
            ttq.setAndDefer=function(t,e){t[e]=function(){t.push([e].concat(Array.prototype.slice.call(arguments,0)))}};
            for(var i=0;i<ttq.methods.length;i++)ttq.setAndDefer(ttq,ttq.methods[i]);
            ttq.instance=function(t){for(var e=ttq._i[t]||[],n=0;n<ttq.methods.length;n++)ttq.setAndDefer(e,ttq.methods[n]);return e};
            ttq.load=function(e,n){var r="https://analytics.tiktok.com/i18n/pixel/events.js",o=n&&n.partner;
            ttq._i=ttq._i||{},ttq._i[e]=[],ttq._i[e]._u=r,ttq._t=ttq._t||{},ttq._t[e]=+new Date,ttq._o=ttq._o||{},ttq._o[e]=n||{};
            var i=document.createElement("script");i.type="text/javascript",i.async=!0,i.src=r+"?sdkid="+e+"&lib="+t;
            var a=document.getElementsByTagName("script")[0];a.parentNode.insertBefore(i,a)};
            ttq.load('${pixelId}');
            ttq.page();
          }(window, document, 'ttq');
        `,
      }}
    />
  )
}
```

### 2.4 أماكن إطلاق الأحداث

نفس أماكن Facebook Pixel بالظبط — مع استدعاء الدوال المقابلة من `tiktok-pixel.ts`.

---

## 3. Google Analytics 4

### 3.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | أداة تحليل الزيارات من Google — يعطي التاجر بيانات عن مصادر الزيارات وسلوك العملاء |
| **ليه مهم** | التاجر يقدر يعرف مصادر الترافيك، أشهر المنتجات، معدلات التحويل |
| **Measurement ID** | يبدأ بـ `G-` — التاجر يحصل عليه من Google Analytics console |

### 3.2 الملفات المطلوبة

**ملف**: `src/lib/tracking/google-analytics.ts`

```typescript
/**
 * Google Analytics 4 — Client-Side Tracking
 * 
 * يستخدم gtag() global function.
 * المرجع: https://developers.google.com/analytics/devguides/collection/ga4
 */

function getGtag(): ((...args: unknown[]) => void) | null {
  if (typeof window !== 'undefined' && typeof window.gtag === 'function') {
    return window.gtag
  }
  return null
}

export function gaTrackViewItem(data: {
  itemId: string
  itemName: string
  price: number
  currency: string
  category?: string
}) {
  getGtag()?.('event', 'view_item', {
    currency: data.currency,
    value: data.price,
    items: [{
      item_id: data.itemId,
      item_name: data.itemName,
      price: data.price,
      item_category: data.category,
    }],
  })
}

export function gaTrackAddToCart(data: {
  itemId: string
  itemName: string
  price: number
  currency: string
  quantity: number
}) {
  getGtag()?.('event', 'add_to_cart', {
    currency: data.currency,
    value: data.price * data.quantity,
    items: [{
      item_id: data.itemId,
      item_name: data.itemName,
      price: data.price,
      quantity: data.quantity,
    }],
  })
}

export function gaTrackBeginCheckout(data: {
  value: number
  currency: string
  items: Array<{ id: string; name: string; price: number; quantity: number }>
}) {
  getGtag()?.('event', 'begin_checkout', {
    currency: data.currency,
    value: data.value,
    items: data.items.map(item => ({
      item_id: item.id,
      item_name: item.name,
      price: item.price,
      quantity: item.quantity,
    })),
  })
}

export function gaTrackPurchase(data: {
  transactionId: string
  value: number
  currency: string
  items: Array<{ id: string; name: string; price: number; quantity: number }>
}) {
  getGtag()?.('event', 'purchase', {
    transaction_id: data.transactionId,
    currency: data.currency,
    value: data.value,
    items: data.items.map(item => ({
      item_id: item.id,
      item_name: item.name,
      price: item.price,
      quantity: item.quantity,
    })),
  })
}

export function gaTrackSearch(searchTerm: string) {
  getGtag()?.('event', 'search', { search_term: searchTerm })
}
```

**ملف**: `src/components/tracking/google-analytics-script.tsx`

```typescript
'use client'

import Script from 'next/script'

type GoogleAnalyticsScriptProps = {
  measurementId: string
}

export function GoogleAnalyticsScript({ measurementId }: GoogleAnalyticsScriptProps) {
  return (
    <>
      <Script
        src={`https://www.googletagmanager.com/gtag/js?id=${measurementId}`}
        strategy="afterInteractive"
      />
      <Script
        id="google-analytics"
        strategy="afterInteractive"
        dangerouslySetInnerHTML={{
          __html: `
            window.dataLayer = window.dataLayer || [];
            function gtag(){dataLayer.push(arguments);}
            gtag('js', new Date());
            gtag('config', '${measurementId}');
          `,
        }}
      />
    </>
  )
}
```

### 3.3 مكون مجمّع لكل السكربتات

**ملف**: `src/components/tracking/tracking-scripts.tsx`

```typescript
import { FacebookPixelScript } from './facebook-pixel-script'
import { TikTokPixelScript } from './tiktok-pixel-script'
import { GoogleAnalyticsScript } from './google-analytics-script'

type TrackingScriptsProps = {
  facebookPixelId?: string | null
  tiktokPixelId?: string | null
  googleAnalyticsId?: string | null
}

/**
 * مكون يجمع كل سكربتات التتبع — يُحقن في store/layout.tsx
 * يعرض فقط السكربتات اللي التاجر فعّلها (حطّ ID)
 */
export function TrackingScripts({
  facebookPixelId,
  tiktokPixelId,
  googleAnalyticsId,
}: TrackingScriptsProps) {
  return (
    <>
      {facebookPixelId && <FacebookPixelScript pixelId={facebookPixelId} />}
      {tiktokPixelId && <TikTokPixelScript pixelId={tiktokPixelId} />}
      {googleAnalyticsId && <GoogleAnalyticsScript measurementId={googleAnalyticsId} />}
    </>
  )
}
```

### 3.4 تعديل store/layout.tsx

**الملف**: `src/app/store/layout.tsx`

**إضافة** (بعد `<StoreProvider>` وقبل `<StoreHeader>`):

```typescript
import { TrackingScripts } from '@/components/tracking/tracking-scripts'

// داخل الـ layout function، بعد ما بنجيب store data:
<TrackingScripts
  facebookPixelId={store.settings?.facebookPixelId}
  tiktokPixelId={store.settings?.tiktokPixelId}
  googleAnalyticsId={store.settings?.googleAnalyticsId}
/>
```

---

## 4. WhatsApp Integration

### 4.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | زر WhatsApp عائم + زر طلب عبر واتساب في المنتج |
| **ليه مهم** | WhatsApp الوسيلة رقم 1 للتواصل في مصر. يبني الثقة ويزود التحويلات |
| **التكلفة** | مجاني 100% — يستخدم `wa.me` links (بدون API) |
| **بدون حزم** | لا نحتاج أي حزمة — HTML + CSS فقط |

### 4.2 الملفات المطلوبة

#### مكون الزر العائم

**ملف**: `src/app/store/_components/whatsapp-floating-button.tsx`

```typescript
'use client'

/**
 * زر WhatsApp عائم — يظهر في كل صفحات المتجر
 * 
 * - يقرأ الإعدادات من StoreContext
 * - يفتح wa.me مع رسالة محددة مسبقاً
 * - موضع قابل للتخصيص (يمين/يسار)
 * - أيقونة WhatsApp مع تأثير pulse
 */

import { useStore } from '@/lib/tenant/store-context'

export function WhatsAppFloatingButton() {
  const store = useStore()
  const settings = store.settings
  
  // لو الزر مش مفعّل أو مفيش رقم واتساب، منعرضش حاجة
  if (!settings.whatsappFloatingEnabled) return null
  
  // رقم الواتساب من بيانات المتجر الأساسية (stores.contactWhatsapp)
  // ملاحظة: الرقم ممكن يكون في store.contactWhatsapp
  // لازم يتمرر كـ prop أو يتضاف في StoreContext
  const whatsappNumber = store.whatsappNumber
  if (!whatsappNumber) return null
  
  const defaultMessage = settings.whatsappDefaultMessage
    ?? `مرحباً، أنا أتواصل من متجر ${store.name}`
  
  const encodedMessage = encodeURIComponent(defaultMessage)
  const cleanNumber = whatsappNumber.replace(/[^0-9+]/g, '')
  const whatsappUrl = `https://wa.me/${cleanNumber}?text=${encodedMessage}`
  
  const position = settings.whatsappFloatingPosition ?? 'left'
  const positionClass = position === 'left' ? 'start-4' : 'end-4'

  return (
    <a
      href={whatsappUrl}
      target="_blank"
      rel="noopener noreferrer"
      className={`fixed bottom-6 ${positionClass} z-40 flex h-14 w-14 items-center justify-center rounded-full bg-[#25D366] text-white shadow-lg transition-transform hover:scale-110`}
      aria-label="تواصل عبر واتساب"
    >
      {/* أيقونة WhatsApp SVG */}
      <svg
        xmlns="http://www.w3.org/2000/svg"
        viewBox="0 0 24 24"
        fill="currentColor"
        className="h-7 w-7"
      >
        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" />
      </svg>
    </a>
  )
}
```

#### زر "اطلب عبر واتساب" في صفحة المنتج

**ملف**: `src/app/store/product/[slug]/_components/whatsapp-order-button.tsx`

```typescript
'use client'

import { useStore } from '@/lib/tenant/store-context'

type WhatsAppOrderButtonProps = {
  productName: string
  productPrice: number
  productUrl: string
}

export function WhatsAppOrderButton({
  productName,
  productPrice,
  productUrl,
}: WhatsAppOrderButtonProps) {
  const store = useStore()
  
  if (!store.settings.whatsappOrderButtonEnabled) return null
  
  const whatsappNumber = store.whatsappNumber
  if (!whatsappNumber) return null
  
  const currency = store.settings.currency ?? 'EGP'
  const message = `مرحباً، أريد طلب المنتج التالي:\n\n📦 ${productName}\n💰 ${productPrice} ${currency}\n🔗 ${productUrl}`
  
  const encodedMessage = encodeURIComponent(message)
  const cleanNumber = whatsappNumber.replace(/[^0-9+]/g, '')
  const whatsappUrl = `https://wa.me/${cleanNumber}?text=${encodedMessage}`
  
  return (
    <a
      href={whatsappUrl}
      target="_blank"
      rel="noopener noreferrer"
      className="flex w-full items-center justify-center gap-2 rounded-lg bg-[#25D366] px-4 py-3 text-sm font-medium text-white transition-colors hover:bg-[#1da855]"
    >
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" className="h-5 w-5">
        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" />
      </svg>
      اطلب عبر واتساب
    </a>
  )
}
```

### 4.3 تعديل StoreContext

الـ StoreContext الحالي يحتوي على `{ id, slug, name, theme, settings }`. محتاجين نضيف `whatsappNumber`:

**الخيار الأفضل**: إضافة `contactWhatsapp` في البيانات الممررة للـ StoreProvider في `store/layout.tsx`:

```typescript
// في store/layout.tsx، عند تمرير البيانات للـ StoreProvider:
<StoreProvider store={{
  id: store.id,
  slug: store.slug,
  name: store.name,
  theme: store.theme,
  settings: store.settings,
  whatsappNumber: store.contactWhatsapp, // جديد
}}>
```

### 4.4 إعدادات الداشبورد

قسم جديد في Settings أو ضمن قسم الـ Tracking:

```
📱 إعدادات WhatsApp
├── [✓] تفعيل زر WhatsApp العائم
├── موضع الزر:  ○ يسار  ● يمين
├── الرسالة الافتراضية: [مرحباً، أنا أتواصل من متجر...]
├── [  ] تفعيل زر "اطلب عبر واتساب" في صفحة المنتج
└── [حفظ]
```

---

## 5. Transactional Emails (Resend)

### 5.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | إيميلات تلقائية تتبعت للعميل والتاجر عند أحداث معينة (طلب جديد، تأكيد دفع، شحن) |
| **الخدمة** | **Resend** — أفضل خدمة إيميلات للمطورين |
| **السعر** | مجاني حتى 3,000 إيميل/شهر (100/يوم)، بعدها $20/شهر لـ 50,000 |
| **الحزم** | `resend` (SDK) + `@react-email/components` (templates) |
| **البديل** | Brevo (مجاني 300/يوم) لو Resend مش مناسب |

### 5.2 لماذا Resend؟

- **Next.js native** — SDK رسمي + React Email templates
- **سهولة التكامل** — 5 أسطر كود لإرسال إيميل
- **مجاني كافي** — 3,000 إيميل/شهر يكفي لأول 100+ تاجر
- **React Email** — يكتب templates في React عادي (مش HTML قديم)
- **Webhook tracking** — يقدر يتتبع opens و clicks

### 5.3 Setup

**الحزم المطلوبة**:
```bash
npm install resend @react-email/components --legacy-peer-deps
```

**المتغيرات البيئية**:
```env
# Resend
RESEND_API_KEY=re_xxxxxxxxxxxx
RESEND_FROM_EMAIL=noreply@matjary.com
# أو noreply@mail.matjary.com بعد ضبط DNS
```

**ضبط DNS** (مرة واحدة في Vercel/Domain):
- إضافة MX + TXT records اللي Resend بيديهم لك عند ربط الدومين
- تأكيد الدومين من Resend dashboard

### 5.4 بنية الملفات

```
src/
├── lib/
│   └── email/
│       ├── resend.ts              # Resend client instance + send helpers
│       └── templates/
│           ├── order-confirmation.tsx   # تأكيد طلب للعميل
│           ├── payment-confirmation.tsx # تأكيد دفع للعميل
│           ├── order-shipped.tsx        # إشعار شحن للعميل
│           ├── order-delivered.tsx      # تأكيد توصيل للعميل
│           └── new-order-merchant.tsx   # إشعار طلب جديد للتاجر
```

### 5.5 التنفيذ

#### Resend Client

**ملف**: `src/lib/email/resend.ts`

```typescript
/**
 * Resend Email Service
 * 
 * يُستخدم لإرسال إيميلات تلقائية (Transactional Emails).
 * يُستدعى من API Routes فقط (server-side).
 */

import { Resend } from 'resend'

let resendClient: Resend | null = null

function getResend(): Resend | null {
  const apiKey = process.env.RESEND_API_KEY
  if (!apiKey) {
    console.warn('[Email] RESEND_API_KEY not configured — emails disabled')
    return null
  }
  if (!resendClient) {
    resendClient = new Resend(apiKey)
  }
  return resendClient
}

const FROM_EMAIL = process.env.RESEND_FROM_EMAIL ?? 'noreply@matjary.com'

type SendEmailOptions = {
  to: string | string[]
  subject: string
  react: React.ReactElement
  replyTo?: string
  tags?: Array<{ name: string; value: string }>
}

/**
 * إرسال إيميل عبر Resend
 * 
 * fire-and-forget — لا يعطل أي عملية لو فشل
 */
export async function sendEmail(options: SendEmailOptions): Promise<{ success: boolean; id?: string }> {
  try {
    const resend = getResend()
    if (!resend) return { success: false }

    const { data, error } = await resend.emails.send({
      from: `متجري <${FROM_EMAIL}>`,
      to: Array.isArray(options.to) ? options.to : [options.to],
      subject: options.subject,
      react: options.react,
      reply_to: options.replyTo,
      tags: options.tags,
    })

    if (error) {
      console.error('[Email] Send error:', error)
      return { success: false }
    }

    return { success: true, id: data?.id }
  } catch (error) {
    console.error('[Email] Exception:', error)
    return { success: false }
  }
}
```

#### Email Template — تأكيد الطلب

**ملف**: `src/lib/email/templates/order-confirmation.tsx`

```typescript
/**
 * Template: تأكيد الطلب — يتبعت للعميل بعد إنشاء الطلب
 * 
 * يستخدم @react-email/components لبناء HTML متوافق مع كل الـ email clients.
 * يُعرض بـ React ويتحول لـ HTML تلقائياً بواسطة Resend.
 */

import {
  Html,
  Head,
  Body,
  Container,
  Section,
  Text,
  Heading,
  Hr,
  Row,
  Column,
} from '@react-email/components'

type OrderConfirmationProps = {
  storeName: string
  storeLogo?: string | null
  orderNumber: string
  customerName: string
  items: Array<{
    name: string
    quantity: number
    price: number
    total: number
  }>
  subtotal: number
  shippingCost: number
  discount: number
  total: number
  currency: string
  paymentMethod: 'cod' | 'kashier'
  shippingAddress: string
}

export function OrderConfirmationEmail(props: OrderConfirmationProps) {
  const currencyLabel = props.currency === 'EGP' ? 'ج.م' : props.currency

  return (
    <Html lang="ar" dir="rtl">
      <Head />
      <Body style={{ fontFamily: 'Cairo, Arial, sans-serif', backgroundColor: '#f4f4f5', padding: '20px 0' }}>
        <Container style={{ maxWidth: '600px', margin: '0 auto', backgroundColor: '#ffffff', borderRadius: '8px', padding: '32px' }}>
          
          {/* Header */}
          <Section style={{ textAlign: 'center', marginBottom: '24px' }}>
            <Heading style={{ fontSize: '24px', color: '#1f2937', margin: '0 0 8px' }}>
              تم تأكيد طلبك بنجاح ✅
            </Heading>
            <Text style={{ color: '#6b7280', fontSize: '14px', margin: 0 }}>
              شكراً لتسوقك من {props.storeName}
            </Text>
          </Section>

          {/* Order Info */}
          <Section style={{ backgroundColor: '#f9fafb', borderRadius: '8px', padding: '16px', marginBottom: '24px' }}>
            <Text style={{ margin: '0 0 4px', fontWeight: 'bold' }}>رقم الطلب: #{props.orderNumber}</Text>
            <Text style={{ margin: '0 0 4px' }}>الاسم: {props.customerName}</Text>
            <Text style={{ margin: '0 0 4px' }}>العنوان: {props.shippingAddress}</Text>
            <Text style={{ margin: 0 }}>
              طريقة الدفع: {props.paymentMethod === 'cod' ? 'الدفع عند الاستلام' : 'دفع إلكتروني'}
            </Text>
          </Section>

          <Hr style={{ borderColor: '#e5e7eb' }} />

          {/* Items Table */}
          <Section style={{ marginBottom: '24px' }}>
            <Heading as="h3" style={{ fontSize: '16px', marginBottom: '12px' }}>تفاصيل الطلب</Heading>
            {props.items.map((item, i) => (
              <Row key={i} style={{ marginBottom: '8px' }}>
                <Column style={{ width: '60%' }}>
                  <Text style={{ margin: 0 }}>{item.name} × {item.quantity}</Text>
                </Column>
                <Column style={{ width: '40%', textAlign: 'left' }}>
                  <Text style={{ margin: 0 }}>{item.total} {currencyLabel}</Text>
                </Column>
              </Row>
            ))}
          </Section>

          <Hr style={{ borderColor: '#e5e7eb' }} />

          {/* Totals */}
          <Section style={{ marginBottom: '24px' }}>
            <Row><Column><Text>المجموع الفرعي</Text></Column><Column style={{ textAlign: 'left' }}><Text>{props.subtotal} {currencyLabel}</Text></Column></Row>
            <Row><Column><Text>الشحن</Text></Column><Column style={{ textAlign: 'left' }}><Text>{props.shippingCost} {currencyLabel}</Text></Column></Row>
            {props.discount > 0 && (
              <Row><Column><Text>الخصم</Text></Column><Column style={{ textAlign: 'left' }}><Text style={{ color: '#16a34a' }}>-{props.discount} {currencyLabel}</Text></Column></Row>
            )}
            <Hr style={{ borderColor: '#e5e7eb' }} />
            <Row>
              <Column><Text style={{ fontWeight: 'bold', fontSize: '18px' }}>الإجمالي</Text></Column>
              <Column style={{ textAlign: 'left' }}><Text style={{ fontWeight: 'bold', fontSize: '18px' }}>{props.total} {currencyLabel}</Text></Column>
            </Row>
          </Section>

          {/* Footer */}
          <Section style={{ textAlign: 'center', color: '#9ca3af', fontSize: '12px' }}>
            <Text>هذا الإيميل تم إرساله تلقائياً من {props.storeName} عبر منصة متجري</Text>
          </Section>
        </Container>
      </Body>
    </Html>
  )
}
```

### 5.6 أماكن الاستدعاء

| الإيميل | الملف اللي يستدعيه | المكان بالتحديد |
|---------|-------------------|----------------|
| تأكيد الطلب (للعميل) | `src/app/api/checkout/route.ts` | بعد إنشاء الطلب بنجاح (fire-and-forget) |
| طلب جديد (للتاجر) | `src/app/api/checkout/route.ts` | نفس المكان — يبعت للتاجر كمان |
| تأكيد الدفع | `src/app/api/payments/kashier/webhook/route.ts` | بعد نجاح الدفع |
| تحديث الشحن | `src/app/api/dashboard/orders/[id]/status/route.ts` | عند تغيير الحالة لـ "shipped" |
| تأكيد التوصيل | `src/app/api/dashboard/orders/[id]/status/route.ts` | عند تغيير الحالة لـ "delivered" |

### 5.7 دمج مع Checkout

```typescript
// في src/app/api/checkout/route.ts — بعد إنشاء الطلب:

// === إرسال إيميل تأكيد الطلب (fire-and-forget) ===
if (store.settings?.emailNotificationsEnabled !== false) {
  // للعميل
  if (validatedData.customerEmail) {
    sendEmail({
      to: validatedData.customerEmail,
      subject: `تأكيد طلبك #${orderNumber} من ${store.name}`,
      react: OrderConfirmationEmail({
        storeName: store.name,
        orderNumber,
        customerName: validatedData.customerName,
        items: orderItems.map(i => ({
          name: i.name,
          quantity: i.quantity,
          price: Number(i.price),
          total: Number(i.total),
        })),
        subtotal: Number(subtotal),
        shippingCost: Number(shippingCost),
        discount: Number(discount),
        total: Number(total),
        currency: store.settings?.currency ?? 'EGP',
        paymentMethod: validatedData.paymentMethod,
        shippingAddress: `${validatedData.shipping.area}, ${validatedData.shipping.city}, ${validatedData.shipping.governorate}`,
      }),
    }).catch(() => {})
  }

  // للتاجر
  if (store.settings?.merchantEmailOnNewOrder !== false && store.contactEmail) {
    sendEmail({
      to: store.contactEmail,
      subject: `🛒 طلب جديد #${orderNumber} — ${store.name}`,
      react: NewOrderMerchantEmail({ /* ... */ }),
    }).catch(() => {})
  }
}
```

---

## 6. تصدير الطلبات CSV/Excel

### 6.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | تصدير بيانات الطلبات كملف CSV متوافق مع Excel |
| **ليه مهم** | التجار بيحتاجوا الداتا لشركات الشحن ولسجلاتهم المحاسبية |
| **بدون حزم** | CSV بسيط — `string manipulation` فقط، بدون أي حزمة |
| **الحد الأقصى** | 5,000 طلب في التصدير الواحد (منع DoS) |

### 6.2 الملفات المطلوبة

```
src/
├── lib/
│   └── export/
│       └── csv.ts                     # دوال تحويل JSON → CSV
└── app/
    └── api/
        └── dashboard/
            └── orders/
                └── export/
                    └── route.ts        # GET /api/dashboard/orders/export
```

### 6.3 التنفيذ

#### دالة تحويل CSV

**ملف**: `src/lib/export/csv.ts`

```typescript
/**
 * تحويل مصفوفة كائنات إلى CSV string
 * 
 * - يدعم الحروف العربية (UTF-8 BOM)
 * - يهرب الفواصل والأسطر الجديدة داخل القيم
 * - يُنتج CSV متوافق مع Excel مباشرة
 */

type CsvColumn<T> = {
  header: string         // عنوان العمود (عربي)
  accessor: (row: T) => string | number | null | undefined
}

export function generateCsv<T>(
  data: T[],
  columns: CsvColumn<T>[],
): string {
  // UTF-8 BOM — مطلوب لعرض العربي صح في Excel
  const BOM = '\uFEFF'
  
  // Header row
  const headerRow = columns.map(col => escapeCsvValue(col.header)).join(',')
  
  // Data rows
  const dataRows = data.map(row =>
    columns.map(col => {
      const value = col.accessor(row)
      return escapeCsvValue(value != null ? String(value) : '')
    }).join(',')
  )
  
  return BOM + [headerRow, ...dataRows].join('\r\n')
}

function escapeCsvValue(value: string): string {
  // لو القيمة فيها فاصلة أو سطر جديد أو علامة تنصيص، نلفها في ""
  if (value.includes(',') || value.includes('\n') || value.includes('"')) {
    return `"${value.replace(/"/g, '""')}"`
  }
  return value
}
```

#### API Route للتصدير

**ملف**: `src/app/api/dashboard/orders/export/route.ts`

```typescript
/**
 * GET /api/dashboard/orders/export?status=all&format=csv
 * 
 * يصدّر الطلبات كملف CSV.
 * يطبّق نفس فلاتر صفحة الطلبات (status, search).
 * محمي بـ verifyStoreOwnership().
 */

import { NextRequest } from 'next/server'
import { db } from '@/db'
import { storeOrders, storeOrderItems } from '@/db/schema'
import { eq, and, desc, ilike, or } from 'drizzle-orm'
import { verifyStoreOwnership } from '@/lib/api/auth'
import { apiError, ApiErrors } from '@/lib/api/response'
import { generateCsv } from '@/lib/export/csv'

const MAX_EXPORT_ROWS = 5000

export async function GET(request: NextRequest) {
  try {
    const context = await verifyStoreOwnership(request)
    if (!context) return apiError('غير مصرح', 401)
    const { store } = context

    const url = new URL(request.url)
    const status = url.searchParams.get('status') ?? 'all'
    const search = url.searchParams.get('search') ?? ''

    // بناء الشروط
    const conditions = [eq(storeOrders.storeId, store.id)]
    
    if (status !== 'all') {
      conditions.push(eq(storeOrders.orderStatus, status))
    }
    
    if (search) {
      conditions.push(
        or(
          ilike(storeOrders.orderNumber, `%${search}%`),
          ilike(storeOrders.customerName, `%${search}%`),
          ilike(storeOrders.customerPhone, `%${search}%`),
        )!,
      )
    }

    // جلب الطلبات
    const orders = await db
      .select()
      .from(storeOrders)
      .where(and(...conditions))
      .orderBy(desc(storeOrders.createdAt))
      .limit(MAX_EXPORT_ROWS)

    // تعريف الأعمدة
    const csv = generateCsv(orders, [
      { header: 'رقم الطلب', accessor: r => r.orderNumber },
      { header: 'اسم العميل', accessor: r => r.customerName },
      { header: 'رقم الموبايل', accessor: r => r.customerPhone },
      { header: 'الإيميل', accessor: r => r.customerEmail },
      { header: 'المحافظة', accessor: r => {
        const addr = r.shippingAddress as Record<string, string> | null
        return addr?.governorate ?? ''
      }},
      { header: 'المدينة', accessor: r => {
        const addr = r.shippingAddress as Record<string, string> | null
        return addr?.city ?? ''
      }},
      { header: 'المنطقة', accessor: r => {
        const addr = r.shippingAddress as Record<string, string> | null
        return addr?.area ?? ''
      }},
      { header: 'الشارع', accessor: r => {
        const addr = r.shippingAddress as Record<string, string> | null
        return addr?.street ?? ''
      }},
      { header: 'المجموع الفرعي', accessor: r => r.subtotal },
      { header: 'الشحن', accessor: r => r.shippingCost },
      { header: 'الخصم', accessor: r => r.discount },
      { header: 'الإجمالي', accessor: r => r.total },
      { header: 'كود الخصم', accessor: r => r.couponCode },
      { header: 'طريقة الدفع', accessor: r => r.paymentMethod === 'cod' ? 'عند الاستلام' : 'إلكتروني' },
      { header: 'حالة الدفع', accessor: r => r.paymentStatus },
      { header: 'حالة الطلب', accessor: r => r.orderStatus },
      { header: 'رقم التتبع', accessor: r => r.trackingNumber },
      { header: 'شركة الشحن', accessor: r => r.shippingCompany },
      { header: 'ملاحظات', accessor: r => r.notes },
      { header: 'تاريخ الطلب', accessor: r => r.createdAt ? new Date(r.createdAt).toLocaleDateString('ar-EG') : '' },
    ])

    // إرجاع CSV كملف downloadable
    return new Response(csv, {
      status: 200,
      headers: {
        'Content-Type': 'text/csv; charset=utf-8',
        'Content-Disposition': `attachment; filename="orders-${store.slug}-${new Date().toISOString().split('T')[0]}.csv"`,
      },
    })
  } catch (error) {
    console.error('[Export] Error:', error)
    return apiError('حدث خطأ أثناء التصدير', 500)
  }
}
```

### 6.4 زر التصدير في الداشبورد

يُضاف في صفحة الطلبات (`/dashboard/orders`) — زر بجانب الفلاتر:

```typescript
// في مكون قائمة الطلبات:
<a
  href={`/api/dashboard/orders/export?status=${currentStatus}&search=${searchQuery}`}
  download
  className="inline-flex items-center gap-2 rounded-lg border px-4 py-2 text-sm"
>
  <Download className="h-4 w-4" />
  تصدير CSV
</a>
```

---

## 7. طباعة الفواتير وبوليصات الشحن

### 7.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | صفحات HTML قابلة للطباعة — فاتورة الطلب + بوليصة الشحن |
| **ليه مهم** | كل تاجر محتاج يطبع فاتورة + بوليصة مع كل طلب |
| **الطريقة** | صفحة HTML مخصصة + `@media print` CSS — التاجر يضغط Ctrl+P |
| **بدون حزم** | لا نحتاج PDF library — الطباعة من المتصفح أسهل وأخف |

### 7.2 الملفات المطلوبة

```
src/app/(dashboard)/dashboard/orders/[id]/
├── invoice/
│   └── page.tsx          # صفحة الفاتورة (طباعة)
└── shipping-label/
    └── page.tsx          # صفحة بوليصة الشحن (طباعة)
```

### 7.3 التنفيذ

#### صفحة الفاتورة

**ملف**: `src/app/(dashboard)/dashboard/orders/[id]/invoice/page.tsx`

هذه صفحة Server Component تجلب بيانات الطلب وتعرضه بتنسيق فاتورة.

**المحتوى**:
- شعار المتجر + اسمه + عنوانه + رقم التواصل
- رقم الطلب + التاريخ
- بيانات العميل (الاسم، الرقم، العنوان)
- جدول المنتجات (الاسم، الكمية، سعر الوحدة، الإجمالي)
- ملخص (المجموع الفرعي، الشحن، الخصم، الإجمالي)
- طريقة الدفع + حالة الدفع
- زر "طباعة" (يختفي عند الطباعة)

**CSS المطلوب** (يُضاف في الصفحة أو `globals.css`):
```css
@media print {
  /* إخفاء كل شيء ما عدا محتوى الفاتورة */
  body > *:not(.print-content) { display: none !important; }
  .no-print { display: none !important; }
  .print-content { 
    position: static !important;
    margin: 0 !important;
    padding: 0 !important;
  }
  @page {
    size: A4;
    margin: 15mm;
  }
}
```

**أزرار في صفحة تفاصيل الطلب**:
```
[طباعة فاتورة 🧾]  [طباعة بوليصة شحن 📦]
```

كلاهما `<Link>` يفتح في tab جديد → الصفحة تعمل `window.print()` تلقائياً.

#### صفحة بوليصة الشحن

**ملف**: `src/app/(dashboard)/dashboard/orders/[id]/shipping-label/page.tsx`

**المحتوى** (حجم أصغر — مناسب لطباعة على A5 أو حجم بوليصة):
- **المُرسل**: اسم المتجر + رقم التواصل
- **المُستلم**: اسم العميل + رقم الموبايل + العنوان الكامل
- **المحافظة** (بخط كبير)
- **رقم الطلب** (بخط كبير)
- **ملاحظات الطلب** (لو موجودة)
- **المبلغ المطلوب** (لو COD)

### 7.4 Performance ملاحظة

- الصفحات server-rendered بدون JavaScript — خفيفة جداً
- لا نحتاج PDF generation library (كبيرة وبطيئة)
- التاجر يقدر يحفظ كـ PDF من المتصفح (Print → Save as PDF)

---

## 8. إشعارات الطلبات Real-time

### 8.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | إشعار فوري للتاجر لما يوصل طلب جديد — صوتي + browser push |
| **ليه مهم** | يخلي التاجر يحس إن المنصة "حية" ومحترفة |
| **الطريقة** | Polling كل 30 ثانية (أبسط وأوثق) + Notification API |
| **بدون حزم** | لا نحتاج أي حزمة — `setInterval` + `fetch` + `Notification API` |

### 8.2 لماذا Polling وليس WebSocket/SSE؟

| الطريقة | المميزات | العيوب |
|---------|----------|--------|
| **Polling** | أبسط، يعمل مع Vercel Serverless، لا يحتاج infrastructure | تأخير حتى 30 ثانية |
| **SSE** | Real-time فعلاً | Vercel Serverless لا يدعم long-lived connections |
| **Supabase Realtime** | Real-time + managed | يحتاج setup إضافي |

**القرار**: Polling كل 30 ثانية — أبسط وأوثق. يمكن الترقية لـ Supabase Realtime لاحقاً.

### 8.3 الملفات المطلوبة

```
src/
├── app/
│   └── api/
│       └── dashboard/
│           └── notifications/
│               └── new-orders/
│                   └── route.ts        # GET /api/dashboard/notifications/new-orders
└── components/
    └── dashboard/
        └── order-notification-listener.tsx  # Client component للـ polling
```

### 8.4 التنفيذ

#### API Route — عدد الطلبات الجديدة

**ملف**: `src/app/api/dashboard/notifications/new-orders/route.ts`

```typescript
/**
 * GET /api/dashboard/notifications/new-orders?since=ISO_TIMESTAMP
 * 
 * يرجع عدد الطلبات الجديدة بعد timestamp معين.
 * يُستدعى من مكون الـ polling في الداشبورد.
 */

import { NextRequest } from 'next/server'
import { db } from '@/db'
import { storeOrders } from '@/db/schema'
import { eq, and, gt, count } from 'drizzle-orm'
import { verifyStoreOwnership } from '@/lib/api/auth'
import { apiSuccess, apiError } from '@/lib/api/response'

export async function GET(request: NextRequest) {
  try {
    const context = await verifyStoreOwnership(request)
    if (!context) return apiError('غير مصرح', 401)
    const { store } = context

    const url = new URL(request.url)
    const since = url.searchParams.get('since')
    
    if (!since) return apiError('معامل since مطلوب', 400)

    const sinceDate = new Date(since)
    if (isNaN(sinceDate.getTime())) return apiError('تاريخ غير صالح', 400)

    const [result] = await db
      .select({ count: count() })
      .from(storeOrders)
      .where(and(
        eq(storeOrders.storeId, store.id),
        gt(storeOrders.createdAt, sinceDate),
      ))

    return apiSuccess({
      newOrdersCount: result?.count ?? 0,
      checkedAt: new Date().toISOString(),
    })
  } catch (error) {
    console.error('[Notifications] Error:', error)
    return apiError('حدث خطأ', 500)
  }
}
```

#### مكون الـ Polling في الداشبورد

**ملف**: `src/components/dashboard/order-notification-listener.tsx`

```typescript
'use client'

/**
 * Order Notification Listener
 * 
 * يُحقن في layout الداشبورد.
 * يعمل polling كل 30 ثانية لعدد الطلبات الجديدة.
 * عند وصول طلب جديد:
 *   1. يشغل صوت تنبيه
 *   2. يعرض Browser Notification (لو التاجر سمح)
 *   3. يحدث عداد في الـ UI
 */

import { useEffect, useRef, useCallback, useState } from 'react'

const POLL_INTERVAL = 30_000 // 30 ثانية
const NOTIFICATION_SOUND_URL = '/sounds/new-order.mp3' // ملف صوتي مطلوب

export function OrderNotificationListener() {
  const lastChecked = useRef<string>(new Date().toISOString())
  const audioRef = useRef<HTMLAudioElement | null>(null)
  const [newCount, setNewCount] = useState(0)

  // طلب إذن الإشعارات عند أول تحميل
  useEffect(() => {
    if ('Notification' in window && Notification.permission === 'default') {
      Notification.requestPermission()
    }
    // تحضير الصوت
    audioRef.current = new Audio(NOTIFICATION_SOUND_URL)
    audioRef.current.volume = 0.5
  }, [])

  const checkNewOrders = useCallback(async () => {
    try {
      const res = await fetch(
        `/api/dashboard/notifications/new-orders?since=${encodeURIComponent(lastChecked.current)}`,
      )
      if (!res.ok) return

      const json = await res.json()
      const count = json.data?.newOrdersCount ?? 0

      if (count > 0) {
        setNewCount(prev => prev + count)
        
        // صوت تنبيه
        audioRef.current?.play().catch(() => {})
        
        // Browser notification
        if ('Notification' in window && Notification.permission === 'granted') {
          new Notification('🛒 طلب جديد!', {
            body: `وصل ${count} طلب جديد`,
            icon: '/favicon.ico',
          })
        }
      }

      lastChecked.current = json.data?.checkedAt ?? new Date().toISOString()
    } catch {
      // صامت — لا نزعج التاجر بأخطاء الـ polling
    }
  }, [])

  useEffect(() => {
    const interval = setInterval(checkNewOrders, POLL_INTERVAL)
    return () => clearInterval(interval)
  }, [checkNewOrders])

  // عرض عداد الطلبات الجديدة (لو أكتر من 0)
  // يُعرض كـ badge في sidebar بجانب "الطلبات"
  if (newCount === 0) return null

  return (
    <div className="fixed top-4 end-4 z-50 animate-in fade-in slide-in-from-top-2">
      <a
        href="/dashboard/orders"
        className="flex items-center gap-2 rounded-lg bg-green-500 px-4 py-3 text-sm font-medium text-white shadow-lg"
        onClick={() => setNewCount(0)}
      >
        🛒 {newCount} طلب جديد
      </a>
    </div>
  )
}
```

### 8.5 دمج في Dashboard Layout

**الملف المتأثر**: `src/app/(dashboard)/layout.tsx`

```typescript
import { OrderNotificationListener } from '@/components/dashboard/order-notification-listener'

// داخل الـ layout — بعد <DashboardLayoutShell>:
<OrderNotificationListener />
```

### 8.6 ملف الصوت

يُضاف ملف صوتي في:
```
public/sounds/new-order.mp3
```

ملف MP3 قصير (1-2 ثانية) — صوت "ding" أو "notification bell". يمكن تحميل واحد مجاني من مواقع مثل freesound.org أو pixabay.com/sound-effects.

---

## ملخص الحزم المطلوبة

| الحزمة | الاستخدام | الحجم |
|--------|----------|-------|
| `resend` | إرسال إيميلات | ~50KB |
| `@react-email/components` | React email templates | ~100KB |

**الأمر**:
```bash
npm install resend @react-email/components --legacy-peer-deps
```

**كل باقي الـ P0 (Pixel, TikTok, GA, WhatsApp, CSV, Invoice, Notifications) لا يحتاج أي حزم إضافية.**

---

## ملخص المتغيرات البيئية الجديدة

```env
# Resend (ميزة 5 — Transactional Emails)
RESEND_API_KEY=re_xxxxxxxxxxxx
RESEND_FROM_EMAIL=noreply@matjary.com
```

**ملاحظة**: باقي الإعدادات (Pixel IDs, GA ID, إلخ) تُحفظ في `stores.settings` — لكل متجر إعداداته الخاصة.

---

## ملخص الملفات الجديدة

```
src/
├── lib/
│   ├── tracking/
│   │   ├── types.ts                    # أنواع مشتركة
│   │   ├── facebook-pixel.ts           # FB Pixel client
│   │   ├── facebook-capi.ts            # FB Conversion API server
│   │   ├── tiktok-pixel.ts             # TikTok Pixel client
│   │   └── google-analytics.ts         # GA4 client
│   ├── email/
│   │   ├── resend.ts                   # Resend client + sendEmail()
│   │   └── templates/
│   │       ├── order-confirmation.tsx   # تأكيد طلب (العميل)
│   │       ├── payment-confirmation.tsx # تأكيد دفع (العميل)
│   │       ├── order-shipped.tsx        # إشعار شحن (العميل)
│   │       ├── order-delivered.tsx      # تأكيد توصيل (العميل)
│   │       └── new-order-merchant.tsx   # طلب جديد (التاجر)
│   └── export/
│       └── csv.ts                      # JSON → CSV
├── components/
│   ├── tracking/
│   │   ├── facebook-pixel-script.tsx   # FB Pixel <Script>
│   │   ├── tiktok-pixel-script.tsx     # TikTok <Script>
│   │   ├── google-analytics-script.tsx # GA4 <Script>
│   │   └── tracking-scripts.tsx        # مجمّع
│   └── dashboard/
│       └── order-notification-listener.tsx # Polling + notifications
├── app/
│   ├── store/
│   │   └── _components/
│   │       ├── whatsapp-floating-button.tsx  # زر واتساب عائم
│   │       └── (product page) whatsapp-order-button.tsx
│   └── api/
│       └── dashboard/
│           ├── orders/
│           │   └── export/
│           │       └── route.ts        # GET تصدير CSV
│           └── notifications/
│               └── new-orders/
│                   └── route.ts        # GET عدد طلبات جديدة
│   └── (dashboard)/
│       └── dashboard/
│           └── orders/
│               └── [id]/
│                   ├── invoice/
│                   │   └── page.tsx     # صفحة الفاتورة
│                   └── shipping-label/
│                       └── page.tsx     # صفحة بوليصة الشحن
└── public/
    └── sounds/
        └── new-order.mp3               # صوت إشعار
```

**إجمالي: ~22 ملف جديد + ~5 ملفات تعديل**

---

## الملفات الموجودة المطلوب تعديلها

| الملف | التعديل |
|-------|---------|
| `src/db/schema.ts` | توسيع StoreSettings type بالحقول الجديدة |
| `src/lib/validations/store.ts` | إضافة حقول settings الجديدة في schema |
| `src/app/api/dashboard/settings/route.ts` | السماح بحفظ الحقول الجديدة |
| `src/app/store/layout.tsx` | إضافة `<TrackingScripts />` + `<WhatsAppFloatingButton />` |
| `src/app/api/checkout/route.ts` | إضافة CAPI event + إيميل تأكيد |
| `src/app/api/payments/kashier/webhook/route.ts` | إيميل تأكيد الدفع |
| `src/app/api/dashboard/orders/[id]/status/route.ts` | إيميل شحن/توصيل |
| `src/app/(dashboard)/layout.tsx` | إضافة `<OrderNotificationListener />` |
| `src/lib/tenant/store-context.tsx` | إضافة `whatsappNumber` في النوع |
| `package.json` | إضافة `resend` + `@react-email/components` |

---

## ترتيب التنفيذ المقترح

| # | الميزة | التبعيات | المقدّر |
|---|--------|----------|---------|
| 1 | تعديل StoreSettings type + validation | لا شيء | أساس لكل شيء |
| 2 | تعديل Settings page في الداشبورد | #1 | UI لحفظ Pixel IDs |
| 3 | Facebook Pixel (client-side) | #1, #2 | يُختبر فوراً بـ Pixel Helper |
| 4 | TikTok Pixel | #1, #2, #3 (نفس النمط) | سريع |
| 5 | Google Analytics | #1, #2 | سريع |
| 6 | Facebook Conversion API | #3 | يُدمج في checkout |
| 7 | WhatsApp Integration | #1, #2 | مستقل |
| 8 | Transactional Emails (Resend) | مستقل (resend package) | أكبر قطعة |
| 9 | تصدير CSV | مستقل | سريع |
| 10 | طباعة فواتير + بوليصات | مستقل | متوسط |
| 11 | إشعارات Real-time | مستقل | سريع |

---

> **نهاية الملف** — آخر تحديث: 25 مارس 2026

# دليل تنفيذ المرحلة 3 (P3) — مميزات متقدمة وتوسع

> **تاريخ الإنشاء**: يوليو 2025  
> **المرجعية**: خطة-المميزات-الجديدة.md  
> **ما قبلها**: تنفيذ-المرحلة-2-P2.md (مكتمل ✅)  
> **الهدف**: دليل تنفيذ تفصيلي لكل ميزة من مميزات P3 التسعة  
> **المستوى**: جاهز للتنفيذ (Copy-Paste Ready)

---

## الفهرس

1. [نظام الكوبونات المتقدمة (Advanced Coupons)](#1-نظام-الكوبونات-المتقدمة-advanced-coupons)
2. [مدونة التاجر (Store Blog)](#2-مدونة-التاجر-store-blog)
3. [PWA (Progressive Web App)](#3-pwa-progressive-web-app)
4. [نظام النقاط والولاء (Loyalty Program)](#4-نظام-النقاط-والولاء-loyalty-program)
5. [نظام التسويق بالعمولة (Affiliate System)](#5-نظام-التسويق-بالعمولة-affiliate-system)
6. [نظام Dropshipping المدمج](#6-نظام-dropshipping-المدمج)
7. [تكامل شركات الشحن المصرية](#7-تكامل-شركات-الشحن-المصرية)
8. [Multi-language Support (دعم لغات متعددة)](#8-multi-language-support-دعم-لغات-متعددة)
9. [تطبيق موبايل أصلي (Mobile App Planning)](#9-تطبيق-موبايل-أصلي-mobile-app-planning)

---

## ملخص الترتيب والأولوية

| # | الميزة | الجهد | يعتمد على |
|---|--------|-------|-----------|
| 1 | كوبونات متقدمة | منخفض | توسيع `storeCoupons` الحالي |
| 2 | مدونة التاجر | متوسط | جدول جديد + صفحات Dashboard + Storefront |
| 3 | PWA | منخفض | manifest.json + service worker |
| 4 | نظام الولاء | متوسط-عالي | جداول + إعدادات + تعديل checkout |
| 5 | نظام الأفلييت | عالي | جداول + middleware + لوحة تحكم |
| 6 | Dropshipping | عالي | جدول + ربط بالمنتجات + workflow |
| 7 | شركات شحن | عالي | APIs خارجية + webhook |
| 8 | Multi-language | متوسط | تعديل middleware + ترجمات |
| 9 | Mobile App | تخطيط فقط | مشروع منفصل |

> **ترتيب التنفيذ المقترح**: 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 9  
> ابدأ بالأسهل (كوبونات، مدونة، PWA) ثم انتقل للأعقد.

---

## Migration مجمّع لكل P3

**ملف جديد**: `migrations/p3_advanced_features.sql`

```sql
-- =============================================
-- P3: Advanced Features Migration
-- =============================================

-- 1. Advanced Coupons — توسيع الجدول الحالي
ALTER TABLE store_coupons
  ADD COLUMN IF NOT EXISTS first_order_only BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS applicable_product_ids UUID[] DEFAULT '{}',
  ADD COLUMN IF NOT EXISTS applicable_category_ids UUID[] DEFAULT '{}',
  ADD COLUMN IF NOT EXISTS is_free_shipping BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS auto_apply BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS usage_per_customer INTEGER;

-- 2. Store Blog Posts
CREATE TABLE IF NOT EXISTS store_blog_posts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  slug TEXT NOT NULL,
  content TEXT NOT NULL DEFAULT '',
  featured_image TEXT,
  excerpt TEXT,
  author TEXT,
  is_published BOOLEAN NOT NULL DEFAULT false,
  published_at TIMESTAMPTZ,
  seo_title TEXT,
  seo_description TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(store_id, slug)
);
CREATE INDEX IF NOT EXISTS idx_blog_posts_store ON store_blog_posts(store_id);
CREATE INDEX IF NOT EXISTS idx_blog_posts_published ON store_blog_posts(is_published, published_at);

-- 3. Loyalty Points
CREATE TABLE IF NOT EXISTS store_loyalty_points (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  customer_phone TEXT NOT NULL,
  points INTEGER NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('earned', 'redeemed', 'expired', 'adjusted')),
  order_id UUID REFERENCES store_orders(id) ON DELETE SET NULL,
  notes TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_loyalty_store ON store_loyalty_points(store_id);
CREATE INDEX IF NOT EXISTS idx_loyalty_customer ON store_loyalty_points(store_id, customer_phone);

-- 4. Affiliates
CREATE TABLE IF NOT EXISTS store_affiliates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  email TEXT,
  phone TEXT NOT NULL,
  code TEXT NOT NULL,
  commission_rate DECIMAL(5,2) NOT NULL DEFAULT 10.00,
  total_sales DECIMAL(12,2) NOT NULL DEFAULT 0,
  total_commission DECIMAL(12,2) NOT NULL DEFAULT 0,
  pending_commission DECIMAL(12,2) NOT NULL DEFAULT 0,
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(store_id, code)
);
CREATE INDEX IF NOT EXISTS idx_affiliates_store ON store_affiliates(store_id);
CREATE INDEX IF NOT EXISTS idx_affiliates_code ON store_affiliates(store_id, code);

CREATE TABLE IF NOT EXISTS store_affiliate_sales (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  affiliate_id UUID NOT NULL REFERENCES store_affiliates(id) ON DELETE CASCADE,
  order_id UUID NOT NULL REFERENCES store_orders(id) ON DELETE CASCADE,
  sale_amount DECIMAL(12,2) NOT NULL,
  commission_amount DECIMAL(12,2) NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'paid', 'cancelled')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_aff_sales_store ON store_affiliate_sales(store_id);
CREATE INDEX IF NOT EXISTS idx_aff_sales_affiliate ON store_affiliate_sales(affiliate_id);

-- 5. Supplier Products (Dropshipping)
CREATE TABLE IF NOT EXISTS store_supplier_products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  product_id UUID REFERENCES store_products(id) ON DELETE SET NULL,
  supplier_name TEXT NOT NULL,
  supplier_product_url TEXT,
  supplier_price DECIMAL(10,2) NOT NULL,
  retail_price DECIMAL(10,2) NOT NULL,
  auto_order BOOLEAN NOT NULL DEFAULT false,
  lead_time_days INTEGER DEFAULT 7,
  notes TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_supplier_products_store ON store_supplier_products(store_id);
CREATE INDEX IF NOT EXISTS idx_supplier_products_product ON store_supplier_products(product_id);

-- 6. Shipping Company Accounts
CREATE TABLE IF NOT EXISTS store_shipping_accounts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  provider TEXT NOT NULL CHECK (provider IN ('bosta', 'aramex', 'jnt', 'mylerz')),
  api_key TEXT NOT NULL,
  api_secret TEXT,
  account_id TEXT,
  is_active BOOLEAN NOT NULL DEFAULT true,
  settings JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(store_id, provider)
);
CREATE INDEX IF NOT EXISTS idx_shipping_accounts_store ON store_shipping_accounts(store_id);

-- 7. Product Translations (Multi-language)
ALTER TABLE store_products
  ADD COLUMN IF NOT EXISTS translations JSONB DEFAULT '{}';

-- 8. Store Settings — إعدادات P3 (تُضاف في store.settings JSONB)
-- loyaltyEnabled: boolean
-- loyaltyPointsPerEgp: number (مثلاً 1 نقطة لكل 10 جنيه)
-- loyaltyPointValue: number (مثلاً 1 نقطة = 0.5 جنيه)
-- loyaltyMinRedemption: number (أقل عدد نقاط للاستبدال)
-- affiliateEnabled: boolean
-- affiliateDefaultCommission: number
-- dropshippingEnabled: boolean
-- blogEnabled: boolean
-- pwaEnabled: boolean
-- defaultLanguage: 'ar' | 'en'
-- supportedLanguages: string[]
```

---

## Drizzle Schema — كل الجداول الجديدة

**يُضاف في**: `src/db/schema.ts`

```typescript
// ============================================
// 20. STORE BLOG POSTS (P3 — Blog)
// ============================================
export const storeBlogPosts = pgTable('store_blog_posts', {
  id: uuid('id').defaultRandom().primaryKey(),
  storeId: uuid('store_id')
    .references(() => stores.id, { onDelete: 'cascade' })
    .notNull(),
  title: text('title').notNull(),
  slug: text('slug').notNull(),
  content: text('content').notNull().default(''),
  featuredImage: text('featured_image'),
  excerpt: text('excerpt'),
  author: text('author'),
  isPublished: boolean('is_published').default(false).notNull(),
  publishedAt: timestamp('published_at', { withTimezone: true }),
  seoTitle: text('seo_title'),
  seoDescription: text('seo_description'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
}, (table) => [
  uniqueIndex('uniq_blog_store_slug').on(table.storeId, table.slug),
  index('idx_blog_posts_store').on(table.storeId),
  index('idx_blog_posts_published').on(table.isPublished, table.publishedAt),
])

// ============================================
// 21. STORE LOYALTY POINTS (P3 — Loyalty)
// ============================================
export const storeLoyaltyPoints = pgTable('store_loyalty_points', {
  id: uuid('id').defaultRandom().primaryKey(),
  storeId: uuid('store_id')
    .references(() => stores.id, { onDelete: 'cascade' })
    .notNull(),
  customerPhone: text('customer_phone').notNull(),
  points: integer('points').notNull(),
  type: text('type', {
    enum: ['earned', 'redeemed', 'expired', 'adjusted'],
  }).notNull(),
  orderId: uuid('order_id')
    .references(() => storeOrders.id, { onDelete: 'set null' }),
  notes: text('notes'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
}, (table) => [
  index('idx_loyalty_store').on(table.storeId),
  index('idx_loyalty_customer').on(table.storeId, table.customerPhone),
])

// ============================================
// 22. STORE AFFILIATES (P3 — Affiliate)
// ============================================
export const storeAffiliates = pgTable('store_affiliates', {
  id: uuid('id').defaultRandom().primaryKey(),
  storeId: uuid('store_id')
    .references(() => stores.id, { onDelete: 'cascade' })
    .notNull(),
  name: text('name').notNull(),
  email: text('email'),
  phone: text('phone').notNull(),
  code: text('code').notNull(),
  commissionRate: decimal('commission_rate', { precision: 5, scale: 2 }).notNull().default('10.00'),
  totalSales: decimal('total_sales', { precision: 12, scale: 2 }).notNull().default('0'),
  totalCommission: decimal('total_commission', { precision: 12, scale: 2 }).notNull().default('0'),
  pendingCommission: decimal('pending_commission', { precision: 12, scale: 2 }).notNull().default('0'),
  isActive: boolean('is_active').default(true).notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
}, (table) => [
  uniqueIndex('uniq_affiliate_store_code').on(table.storeId, table.code),
  index('idx_affiliates_store').on(table.storeId),
])

// ============================================
// 23. STORE AFFILIATE SALES (P3 — Affiliate)
// ============================================
export const storeAffiliateSales = pgTable('store_affiliate_sales', {
  id: uuid('id').defaultRandom().primaryKey(),
  storeId: uuid('store_id')
    .references(() => stores.id, { onDelete: 'cascade' })
    .notNull(),
  affiliateId: uuid('affiliate_id')
    .references(() => storeAffiliates.id, { onDelete: 'cascade' })
    .notNull(),
  orderId: uuid('order_id')
    .references(() => storeOrders.id, { onDelete: 'cascade' })
    .notNull(),
  saleAmount: decimal('sale_amount', { precision: 12, scale: 2 }).notNull(),
  commissionAmount: decimal('commission_amount', { precision: 12, scale: 2 }).notNull(),
  status: text('status', {
    enum: ['pending', 'approved', 'paid', 'cancelled'],
  }).notNull().default('pending'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
}, (table) => [
  index('idx_aff_sales_store').on(table.storeId),
  index('idx_aff_sales_affiliate').on(table.affiliateId),
])

// ============================================
// 24. STORE SUPPLIER PRODUCTS (P3 — Dropshipping)
// ============================================
export const storeSupplierProducts = pgTable('store_supplier_products', {
  id: uuid('id').defaultRandom().primaryKey(),
  storeId: uuid('store_id')
    .references(() => stores.id, { onDelete: 'cascade' })
    .notNull(),
  productId: uuid('product_id')
    .references(() => storeProducts.id, { onDelete: 'set null' }),
  supplierName: text('supplier_name').notNull(),
  supplierProductUrl: text('supplier_product_url'),
  supplierPrice: decimal('supplier_price', { precision: 10, scale: 2 }).notNull(),
  retailPrice: decimal('retail_price', { precision: 10, scale: 2 }).notNull(),
  autoOrder: boolean('auto_order').default(false).notNull(),
  leadTimeDays: integer('lead_time_days').default(7),
  notes: text('notes'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
}, (table) => [
  index('idx_supplier_products_store').on(table.storeId),
  index('idx_supplier_products_product').on(table.productId),
])

// ============================================
// 25. STORE SHIPPING ACCOUNTS (P3 — Shipping Companies)
// ============================================
export const storeShippingAccounts = pgTable('store_shipping_accounts', {
  id: uuid('id').defaultRandom().primaryKey(),
  storeId: uuid('store_id')
    .references(() => stores.id, { onDelete: 'cascade' })
    .notNull(),
  provider: text('provider', {
    enum: ['bosta', 'aramex', 'jnt', 'mylerz'],
  }).notNull(),
  apiKey: text('api_key').notNull(),
  apiSecret: text('api_secret'),
  accountId: text('account_id'),
  isActive: boolean('is_active').default(true).notNull(),
  settings: jsonb('settings').$type<Record<string, unknown>>().default({}),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
}, (table) => [
  uniqueIndex('uniq_shipping_account_store_provider').on(table.storeId, table.provider),
  index('idx_shipping_accounts_store').on(table.storeId),
])
```

### تعديل `storeCoupons` الحالي

```typescript
// يُعدَّل في تعريف storeCoupons الحالي — أضف هذه الأعمدة:
  firstOrderOnly: boolean('first_order_only').default(false).notNull(),
  applicableProductIds: text('applicable_product_ids').array().$type<string[]>().default([]),
  applicableCategoryIds: text('applicable_category_ids').array().$type<string[]>().default([]),
  isFreeShipping: boolean('is_free_shipping').default(false).notNull(),
  autoApply: boolean('auto_apply').default(false).notNull(),
  usagePerCustomer: integer('usage_per_customer'),
```

### تعديل `storeProducts` الحالي

```typescript
// يُضاف عمود في storeProducts:
  translations: jsonb('translations').$type<Record<string, { name?: string; description?: string; shortDescription?: string }>>().default({}),
```

### تعديل sidebar في Dashboard

**الملف**: `src/app/(dashboard)/dashboard-layout-shell.tsx`

أضف في `DashboardSidebarIcon`:
```typescript
| 'blog'
| 'affiliate'
| 'loyalty'
```

أضف في `iconMap`:
```typescript
import { BookOpen, Share2, Gift } from 'lucide-react'

  blog: <BookOpen className="h-4 w-4" />,
  affiliate: <Share2 className="h-4 w-4" />,
  loyalty: <Gift className="h-4 w-4" />,
```

أضف في `sidebarItems` بالملف `src/app/(dashboard)/layout.tsx`:
```typescript
  { href: '/dashboard/blog', label: 'المدونة', icon: 'blog' as const },
  { href: '/dashboard/affiliates', label: 'الأفلييت', icon: 'affiliate' as const },
  { href: '/dashboard/loyalty', label: 'نقاط الولاء', icon: 'loyalty' as const },
```

---

## 1. نظام الكوبونات المتقدمة (Advanced Coupons)

### 1.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | توسيع نظام الكوبونات الحالي بخصائص متقدمة: كوبون أول طلب، كوبون لمنتجات/تصنيفات محددة، شحن مجاني، تطبيق تلقائي |
| **ليه مهم** | التجار يطلبوا كوبونات أكثر مرونة — وده موجود في كل منافس |
| **الجهد** | منخفض — توسيع schema + تعديل validation |

### 1.2 تعديل Schema الحالي

**الموجود حالياً في** `storeCoupons`:
- `code`, `type` ('percentage' | 'fixed'), `value`, `minOrderAmount`, `maxDiscount`
- `usageLimit`, `usedCount`, `startsAt`, `expiresAt`, `isActive`

**الأعمدة الجديدة** (شرح كل واحد):

| العمود | النوع | الوصف |
|--------|-------|-------|
| `firstOrderOnly` | boolean | الكوبون ينطبق على أول طلب فقط لكل عميل |
| `applicableProductIds` | text[] | قائمة IDs المنتجات اللي الكوبون بيشتغل عليها (فارغ = كل المنتجات) |
| `applicableCategoryIds` | text[] | قائمة IDs التصنيفات (فارغ = كل التصنيفات) |
| `isFreeShipping` | boolean | كوبون شحن مجاني (بيلغي تكلفة الشحن) |
| `autoApply` | boolean | ينطبق تلقائياً بدون ما العميل يدخل كود |
| `usagePerCustomer` | integer | أقصى عدد استخدامات لكل عميل (بالـ phone) |

### 1.3 تعديل Validation

**الملف**: `src/lib/validations/order.ts`

```typescript
// أضف الحقول الجديدة في coupon validation schema:

export const couponSchema = z.object({
  code: z.string().min(2).max(30).regex(/^[A-Za-z0-9_-]+$/),
  type: z.enum(['percentage', 'fixed', 'free_shipping']),
  value: z.coerce.number().min(0),
  minOrderAmount: z.coerce.number().min(0).optional().nullable(),
  maxDiscount: z.coerce.number().min(0).optional().nullable(),
  usageLimit: z.coerce.number().int().min(1).optional().nullable(),
  startsAt: z.coerce.date().optional().nullable(),
  expiresAt: z.coerce.date().optional().nullable(),
  isActive: z.boolean().default(true),
  // === P3 الجديد ===
  firstOrderOnly: z.boolean().default(false),
  applicableProductIds: z.array(z.string().uuid()).default([]),
  applicableCategoryIds: z.array(z.string().uuid()).default([]),
  isFreeShipping: z.boolean().default(false),
  autoApply: z.boolean().default(false),
  usagePerCustomer: z.coerce.number().int().min(1).optional().nullable(),
})
```

### 1.4 تعديل API للـ Validate

**الملف**: `src/app/api/coupons/validate/route.ts`

```typescript
// التحققات الإضافية في POST handler بعد التحققات الحالية:

// 1. تحقق من أول طلب
if (coupon.firstOrderOnly) {
  const existingOrders = await db
    .select({ count: sql<number>`count(*)::int` })
    .from(storeOrders)
    .where(and(
      eq(storeOrders.storeId, storeId),
      eq(storeOrders.customerPhone, customerPhone),
      ne(storeOrders.orderStatus, 'cancelled'),
    ))
  
  if ((existingOrders[0]?.count ?? 0) > 0) {
    return apiError('هذا الكوبون متاح لأول طلب فقط', 400)
  }
}

// 2. تحقق من حد الاستخدام لكل عميل
if (coupon.usagePerCustomer) {
  const customerUsage = await db
    .select({ count: sql<number>`count(*)::int` })
    .from(storeOrders)
    .where(and(
      eq(storeOrders.storeId, storeId),
      eq(storeOrders.customerPhone, customerPhone),
      eq(storeOrders.couponCode, coupon.code),
    ))
  
  if ((customerUsage[0]?.count ?? 0) >= coupon.usagePerCustomer) {
    return apiError('تم استخدام هذا الكوبون بالحد الأقصى المسموح لك', 400)
  }
}

// 3. تحقق من المنتجات المحددة
if (coupon.applicableProductIds.length > 0) {
  const applicableItems = cartItems.filter(item =>
    coupon.applicableProductIds.includes(item.productId)
  )
  if (applicableItems.length === 0) {
    return apiError('هذا الكوبون لا ينطبق على المنتجات في السلة', 400)
  }
  // الخصم يحسب على المنتجات المؤهلة فقط
  subtotalForDiscount = applicableItems.reduce((sum, item) => sum + item.total, 0)
}

// 4. تحقق من التصنيفات المحددة (بنفس المنطق)
if (coupon.applicableCategoryIds.length > 0) {
  // تحقق إن على الأقل منتج واحد في السلة من التصنيفات المؤهلة
  // يحتاج join مع storeProducts للحصول على categoryId
}

// 5. شحن مجاني
if (coupon.isFreeShipping) {
  // بدل ما يرجع discount amount، يرجع:
  return apiSuccess({
    valid: true,
    type: 'free_shipping',
    shippingDiscount: shippingCost,
    code: coupon.code,
  })
}
```

### 1.5 Auto-Apply Logic

**ملف جديد**: `src/lib/coupons/auto-apply.ts`

```typescript
import { db } from '@/db'
import { storeCoupons } from '@/db/schema'
import { and, eq, lte, gte, sql } from 'drizzle-orm'

/**
 * يبحث عن كوبونات تلقائية تنطبق على السلة
 */
export async function findAutoApplyCoupons(
  storeId: string,
  subtotal: number,
  customerPhone: string,
) {
  const now = new Date()

  const coupons = await db
    .select()
    .from(storeCoupons)
    .where(and(
      eq(storeCoupons.storeId, storeId),
      eq(storeCoupons.isActive, true),
      eq(storeCoupons.autoApply, true),
      // لم يتجاوز حد الاستخدام
      sql`(${storeCoupons.usageLimit} IS NULL OR ${storeCoupons.usedCount} < ${storeCoupons.usageLimit})`,
      // في النطاق الزمني
      sql`(${storeCoupons.startsAt} IS NULL OR ${storeCoupons.startsAt} <= ${now})`,
      sql`(${storeCoupons.expiresAt} IS NULL OR ${storeCoupons.expiresAt} >= ${now})`,
    ))

  // مرتبة حسب أكبر خصم — نرجع أفضل كوبون فقط
  return coupons.filter(c => {
    if (c.minOrderAmount && subtotal < Number(c.minOrderAmount)) return false
    return true
  })
}
```

### 1.6 تعديل واجهة Dashboard الكوبون

**الملفات المتأثرة**:

- `src/app/(dashboard)/dashboard/coupons/` — أضف الحقول الجديدة في فورم إنشاء/تعديل الكوبون:
  - Toggle: "أول طلب فقط"
  - Toggle: "شحن مجاني"
  - Toggle: "تطبيق تلقائي"
  - Multi-select: منتجات محددة (اختياري)
  - Multi-select: تصنيفات محددة (اختياري)
  - Input: حد الاستخدام لكل عميل

### 1.7 تعديل Storefront Cart

- عند تحميل صفحة Checkout، استدعي `findAutoApplyCoupons()` وطبّق أفضل كوبون تلقائي
- أظهر رسالة: "🎉 تم تطبيق كوبون خصم تلقائياً!"
- لو العميل أدخل كوبون يدوي، الكوبون اليدوي يكون له أولوية على التلقائي

---

## 2. مدونة التاجر (Store Blog)

### 2.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | كل تاجر يقدر ينشر مقالات في متجره لتحسين SEO وجذب زيارات |
| **ليه مهم** | Content Marketing أساسي — المنافسين معندهمش مدونة مدمجة |
| **الجهد** | متوسط |

### 2.2 بنية الملفات

```
src/app/
├── (dashboard)/dashboard/blog/
│   ├── page.tsx                    # قائمة المقالات
│   ├── new/
│   │   └── page.tsx                # إنشاء مقالة جديدة
│   └── [id]/
│       └── edit/
│           └── page.tsx            # تعديل مقالة
│
└── store/blog/
    ├── page.tsx                    # قائمة المقالات في المتجر
    └── [slug]/
        └── page.tsx                # صفحة مقالة واحدة
```

### 2.3 API Routes

**ملف**: `src/app/api/dashboard/blog/route.ts`

```typescript
import { NextRequest } from 'next/server'
import { db } from '@/db'
import { storeBlogPosts } from '@/db/schema'
import { apiSuccess, handleApiError, ApiErrors } from '@/lib/api/response'
import { verifyStoreOwnership } from '@/lib/api/verify-store'
import { eq, desc } from 'drizzle-orm'
import { z } from 'zod'

const blogPostSchema = z.object({
  title: z.string().min(1, { error: 'العنوان مطلوب' }).max(200),
  slug: z.string().min(1).max(200).regex(/^[a-z0-9-]+$/, { error: 'الرابط يحتوي على أحرف صغيرة وأرقام وشرطات فقط' }),
  content: z.string().default(''),
  featuredImage: z.string().url().optional().nullable(),
  excerpt: z.string().max(500).optional().nullable(),
  author: z.string().max(100).optional().nullable(),
  isPublished: z.boolean().default(false),
  seoTitle: z.string().max(70).optional().nullable(),
  seoDescription: z.string().max(160).optional().nullable(),
})

// GET — قائمة المقالات
export async function GET(request: NextRequest) {
  try {
    const { storeId } = await verifyStoreOwnership(request)

    const posts = await db
      .select()
      .from(storeBlogPosts)
      .where(eq(storeBlogPosts.storeId, storeId))
      .orderBy(desc(storeBlogPosts.createdAt))

    return apiSuccess(posts)
  } catch (error) {
    return handleApiError(error)
  }
}

// POST — إنشاء مقالة
export async function POST(request: NextRequest) {
  try {
    const { storeId } = await verifyStoreOwnership(request)
    const body = await request.json()
    const data = blogPostSchema.parse(body)

    const [post] = await db
      .insert(storeBlogPosts)
      .values({
        storeId,
        ...data,
        publishedAt: data.isPublished ? new Date() : null,
      })
      .returning()

    return apiSuccess(post, 201)
  } catch (error) {
    return handleApiError(error)
  }
}
```

**ملف**: `src/app/api/dashboard/blog/[id]/route.ts`

```typescript
// GET — مقالة واحدة
// PUT — تعديل مقالة (نفس schema + id param)
// DELETE — حذف مقالة
// نفس نمط الملفات الموجودة في /api/dashboard/
```

**ملف**: `src/app/api/storefront/blog/route.ts`

```typescript
// GET — المقالات المنشورة للمتجر (عامة بدون auth)
// filter: isPublished = true, ordered by publishedAt DESC
// يستقبل storeId من x-store-slug header
```

**ملف**: `src/app/api/storefront/blog/[slug]/route.ts`

```typescript
// GET — مقالة واحدة بالـ slug (عامة)
// filter: isPublished = true AND slug = param AND storeId
```

### 2.4 صفحة Dashboard — قائمة المقالات

**ملف**: `src/app/(dashboard)/dashboard/blog/page.tsx`

```typescript
// Server Component
// يعرض جدول بالمقالات: العنوان، الحالة (مسودة/منشور)، التاريخ، الإجراءات
// زر "مقالة جديدة" يوجه لـ /dashboard/blog/new
// نفس نمط صفحة /dashboard/coupons أو /dashboard/pages
```

### 2.5 صفحة Dashboard — إنشاء/تعديل مقالة

```
الفورم يحتوي:
├── العنوان (text input)
├── الرابط/Slug (auto-generated من العنوان + editable)
├── المحتوى (rich text editor — استخدم textarea أو محرر بسيط)
│   └── زر "✨ اكتب بالذكاء الاصطناعي" (يستدعي OpenAI — مرتبط بميزة P2)
├── الصورة البارزة (upload أو URL)
├── المقتطف (textarea قصير)
├── اسم الكاتب (text)
├── SEO:
│   ├── عنوان SEO
│   └── وصف SEO
└── زر النشر (toggle: مسودة/منشور)
```

**ملاحظة AI**: لتوليد المحتوى بالذكاء الاصطناعي:

```typescript
// POST /api/dashboard/blog/generate
// يستقبل: { title, keywords, tone } 
// يستخدم openai client من P2 (src/lib/ai/openai-client.ts)
// يرجع: { content, excerpt, seoTitle, seoDescription }

const prompt = `أنت كاتب محتوى محترف. اكتب مقالة مدونة عن "${title}".
الكلمات المفتاحية: ${keywords}
النبرة: ${tone}
اكتب بالعربية. المقالة لازم تكون:
- 500-800 كلمة
- مقسمة لأقسام بعناوين فرعية
- تحتوي على نصائح عملية
- محسنة لمحركات البحث

أرجع الناتج بصيغة JSON:
{
  "content": "HTML content...",
  "excerpt": "ملخص قصير 2-3 أسطر",
  "seoTitle": "عنوان SEO (أقل من 60 حرف)",
  "seoDescription": "وصف SEO (أقل من 160 حرف)"
}`
```

### 2.6 صفحات Storefront — المدونة

**ملف**: `src/app/store/blog/page.tsx`

```typescript
// Server Component — قائمة المقالات المنشورة
// يعرض: بطاقات المقالات (صورة + عنوان + مقتطف + تاريخ)
// Pagination (اختياري)
// generateMetadata لـ SEO

import { Metadata } from 'next'

export async function generateMetadata({ searchParams }: Props): Promise<Metadata> {
  return {
    title: `المدونة | ${storeName}`,
    description: `آخر المقالات والأخبار من ${storeName}`,
  }
}
```

**ملف**: `src/app/store/blog/[slug]/page.tsx`

```typescript
// Server Component — مقالة واحدة
// يعرض: العنوان، الصورة البارزة، المحتوى، التاريخ، الكاتب
// generateMetadata من seoTitle/seoDescription
// Structured Data (JSON-LD Article)
// أزرار مشاركة (WhatsApp, Facebook, Twitter, Copy Link)

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  // fetch blog post by slug
  return {
    title: post.seoTitle || post.title,
    description: post.seoDescription || post.excerpt,
    openGraph: {
      title: post.seoTitle || post.title,
      description: post.seoDescription || post.excerpt,
      images: post.featuredImage ? [post.featuredImage] : [],
      type: 'article',
      publishedTime: post.publishedAt?.toISOString(),
    },
  }
}
```

### 2.7 رابط المدونة في Header المتجر

**الملف**: `src/app/store/_components/store-header.tsx`

```typescript
// أضف رابط "المدونة" بعد التصنيفات في الـ navigation
// يظهر بس لو المتجر عنده blogEnabled في الإعدادات
<Link href="/store/blog">المدونة</Link>
```

---

## 3. PWA (Progressive Web App)

### 3.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | كل متجر يتحول لـ PWA — يقدر العميل يثبته على موبايله كأنه تطبيق |
| **ليه مهم** | 90% من التجار المصريين بيشتغلوا من الموبايل |
| **الجهد** | منخفض |

### 3.2 Dynamic Manifest

**ملف**: `src/app/store/manifest.json/route.ts` (Route Handler)

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { db } from '@/db'
import { stores } from '@/db/schema'
import { eq } from 'drizzle-orm'

export async function GET(request: NextRequest) {
  const storeSlug = request.headers.get('x-store-slug')
  if (!storeSlug) {
    return NextResponse.json({}, { status: 400 })
  }

  const [store] = await db
    .select()
    .from(stores)
    .where(eq(stores.slug, storeSlug))
    .limit(1)

  if (!store) {
    return NextResponse.json({}, { status: 404 })
  }

  const settings = (store.settings as Record<string, unknown>) ?? {}
  const themeColor = (settings.themeColor as string) || '#6366f1'

  const manifest = {
    name: store.name,
    short_name: store.name.slice(0, 12),
    description: (store.description as string) || `متجر ${store.name}`,
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: themeColor,
    orientation: 'portrait-primary',
    icons: [
      {
        src: (store.logo as string) || '/icons/icon-192.png',
        sizes: '192x192',
        type: 'image/png',
      },
      {
        src: (store.logo as string) || '/icons/icon-512.png',
        sizes: '512x512',
        type: 'image/png',
      },
    ],
  }

  return NextResponse.json(manifest, {
    headers: {
      'Content-Type': 'application/manifest+json',
      'Cache-Control': 'public, max-age=86400',
    },
  })
}
```

### 3.3 Service Worker

**ملف**: `public/sw.js`

```javascript
const CACHE_NAME = 'matjary-store-v1'
const OFFLINE_URL = '/offline'

// الملفات اللي نحفظها في الكاش
const PRECACHE_URLS = [
  '/offline',
]

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(PRECACHE_URLS))
  )
  self.skipWaiting()
})

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(keys.filter((k) => k !== CACHE_NAME).map((k) => caches.delete(k)))
    )
  )
  self.clients.claim()
})

self.addEventListener('fetch', (event) => {
  if (event.request.mode === 'navigate') {
    event.respondWith(
      fetch(event.request).catch(() => caches.match(OFFLINE_URL))
    )
    return
  }

  // Cache-first for static assets
  if (event.request.url.match(/\.(css|js|png|jpg|jpeg|svg|webp|woff2?)$/)) {
    event.respondWith(
      caches.match(event.request).then((cached) =>
        cached || fetch(event.request).then((response) => {
          const clone = response.clone()
          caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone))
          return response
        })
      )
    )
    return
  }

  event.respondWith(fetch(event.request))
})
```

### 3.4 Offline Page

**ملف**: `src/app/store/offline/page.tsx`

```typescript
export default function OfflinePage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center p-6 text-center">
      <div className="text-6xl mb-4">📡</div>
      <h1 className="text-2xl font-bold mb-2">لا يوجد اتصال بالإنترنت</h1>
      <p className="text-muted-foreground mb-4">
        تأكد من اتصالك بالإنترنت وأعد المحاولة
      </p>
      <button
        onClick={() => window.location.reload()}
        className="rounded-lg bg-primary px-6 py-2 text-white"
      >
        إعادة المحاولة
      </button>
    </div>
  )
}
```

### 3.5 تسجيل في Layout المتجر

**الملف**: `src/app/store/layout.tsx`

```typescript
// أضف في <head>:
<link rel="manifest" href="/store/manifest.json" />
<meta name="theme-color" content={themeColor} />
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="default" />

// أضف script لتسجيل Service Worker:
<script
  dangerouslySetInnerHTML={{
    __html: `
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
          navigator.serviceWorker.register('/sw.js')
        })
      }
    `,
  }}
/>
```

### 3.6 Install Banner Component

**ملف**: `src/app/store/_components/pwa-install-banner.tsx`

```typescript
'use client'

import { useState, useEffect } from 'react'

export function PwaInstallBanner() {
  const [deferredPrompt, setDeferredPrompt] = useState<BeforeInstallPromptEvent | null>(null)
  const [showBanner, setShowBanner] = useState(false)

  useEffect(() => {
    const handler = (e: Event) => {
      e.preventDefault()
      setDeferredPrompt(e as BeforeInstallPromptEvent)
      // أظهر البانر بعد 30 ثانية من التصفح
      setTimeout(() => setShowBanner(true), 30000)
    }

    window.addEventListener('beforeinstallprompt', handler)
    return () => window.removeEventListener('beforeinstallprompt', handler)
  }, [])

  const handleInstall = async () => {
    if (!deferredPrompt) return
    deferredPrompt.prompt()
    const { outcome } = await deferredPrompt.userChoice
    if (outcome === 'accepted') {
      setShowBanner(false)
    }
    setDeferredPrompt(null)
  }

  if (!showBanner) return null

  return (
    <div className="fixed bottom-0 inset-x-0 z-50 p-4 bg-background border-t shadow-lg">
      <div className="flex items-center justify-between max-w-md mx-auto">
        <div>
          <p className="font-semibold text-sm">📲 أضف المتجر للشاشة الرئيسية</p>
          <p className="text-xs text-muted-foreground">تصفح أسرع بدون متصفح</p>
        </div>
        <div className="flex gap-2">
          <button
            onClick={() => setShowBanner(false)}
            className="px-3 py-1.5 text-xs text-muted-foreground"
          >
            لاحقاً
          </button>
          <button
            onClick={handleInstall}
            className="px-4 py-1.5 text-xs bg-primary text-primary-foreground rounded-lg font-medium"
          >
            تثبيت
          </button>
        </div>
      </div>
    </div>
  )
}

// TypeScript declaration for BeforeInstallPromptEvent
interface BeforeInstallPromptEvent extends Event {
  prompt(): Promise<void>
  userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>
}
```

### 3.7 أيقونات PWA

ضع في `public/icons/`:
- `icon-192.png` (192×192) — أيقونة افتراضية لمتجري
- `icon-512.png` (512×512) — أيقونة افتراضية لمتجري

> كل متجر يستخدم لوجو المتجر لو موجود، وإلا الأيقونة الافتراضية.

---

## 4. نظام النقاط والولاء (Loyalty Program)

### 4.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | العميل يكسب نقاط مع كل شراء → يستبدلها بخصم في الطلب التالي |
| **ليه مهم** | يزيد معدل العودة (Retention) — العميل عنده حافز يرجع |
| **الجهد** | متوسط-عالي (يلمس checkout + order completion) |

### 4.2 إعدادات الولاء (في store.settings JSONB)

```typescript
// يُضاف في StoreSettings type:
type LoyaltySettings = {
  loyaltyEnabled: boolean            // default: false
  loyaltyPointsPerEgp: number        // مثلاً 1 (1 نقطة لكل 1 جنيه)
  loyaltyPointValue: number           // مثلاً 0.1 (كل نقطة = 0.1 جنيه)
  loyaltyMinRedemption: number        // أقل عدد نقاط للاستبدال (مثلاً 100)
  loyaltyMaxRedemptionPercent: number // أقصى نسبة خصم من الطلب (مثلاً 50%)
}
```

### 4.3 صفحة إعدادات الولاء في Dashboard

**ملف**: `src/app/(dashboard)/dashboard/loyalty/page.tsx`

```
الصفحة تحتوي:
├── Toggle: تفعيل/تعطيل نظام الولاء
├── الإعدادات:
│   ├── معدل كسب النقاط (كام نقطة لكل جنيه)
│   ├── قيمة النقطة (كل نقطة بكام جنيه)
│   ├── أقل عدد نقاط للاستبدال
│   └── أقصى نسبة خصم من الطلب
├── جدول: أحدث عمليات النقاط (العميل، النوع، النقاط، التاريخ)
└── إحصائيات: إجمالي النقاط الممنوحة، المستبدلة، النشطة
```

### 4.4 API — صفحة الولاء في Dashboard

**ملف**: `src/app/api/dashboard/loyalty/route.ts`

```typescript
// GET — إحصائيات + أحدث العمليات
export async function GET(request: NextRequest) {
  const { storeId } = await verifyStoreOwnership(request)

  const transactions = await db
    .select()
    .from(storeLoyaltyPoints)
    .where(eq(storeLoyaltyPoints.storeId, storeId))
    .orderBy(desc(storeLoyaltyPoints.createdAt))
    .limit(50)

  const [stats] = await db
    .select({
      totalEarned: sql<number>`COALESCE(SUM(CASE WHEN type = 'earned' THEN points ELSE 0 END), 0)::int`,
      totalRedeemed: sql<number>`COALESCE(SUM(CASE WHEN type = 'redeemed' THEN ABS(points) ELSE 0 END), 0)::int`,
      totalExpired: sql<number>`COALESCE(SUM(CASE WHEN type = 'expired' THEN ABS(points) ELSE 0 END), 0)::int`,
    })
    .from(storeLoyaltyPoints)
    .where(eq(storeLoyaltyPoints.storeId, storeId))

  return apiSuccess({ transactions, stats })
}
```

### 4.5 API — رصيد العميل

**ملف**: `src/app/api/storefront/loyalty/balance/route.ts`

```typescript
// GET — ?phone=01xxxxxxxxx
// يحسب الرصيد:
// SUM(points) WHERE type='earned' 
// - SUM(ABS(points)) WHERE type='redeemed'
// - SUM(ABS(points)) WHERE type='expired'

export async function GET(request: NextRequest) {
  const storeSlug = request.headers.get('x-store-slug')
  const phone = request.nextUrl.searchParams.get('phone')

  if (!storeSlug || !phone) {
    return ApiErrors.validation('المتجر ورقم الهاتف مطلوبين')
  }

  const store = await getStoreBySlug(storeSlug)
  if (!store) return ApiErrors.storeNotFound()

  const [result] = await db
    .select({
      balance: sql<number>`COALESCE(SUM(points), 0)::int`,
    })
    .from(storeLoyaltyPoints)
    .where(and(
      eq(storeLoyaltyPoints.storeId, store.id),
      eq(storeLoyaltyPoints.customerPhone, phone),
    ))

  const settings = store.settings as LoyaltySettings
  const balance = result?.balance ?? 0
  const valueInEgp = balance * (settings.loyaltyPointValue || 0)

  return apiSuccess({
    points: balance,
    valueInEgp,
    canRedeem: balance >= (settings.loyaltyMinRedemption || 0),
    minRedemption: settings.loyaltyMinRedemption || 0,
  })
}
```

### 4.6 منح النقاط عند إتمام الطلب

**الملف المتأثر**: `src/app/api/dashboard/orders/[id]/status/route.ts`

```typescript
// عند تغيير حالة الطلب لـ 'delivered':
// (بعد updateCustomerTrustScore الموجود حالياً)

if (newStatus === 'delivered') {
  // ... trust score update (موجود) ...

  // P3: منح نقاط ولاء
  const storeSettings = store.settings as LoyaltySettings
  if (storeSettings.loyaltyEnabled && storeSettings.loyaltyPointsPerEgp > 0) {
    const orderTotal = Number(order.totalAmount)
    const pointsToEarn = Math.floor(orderTotal * storeSettings.loyaltyPointsPerEgp)

    if (pointsToEarn > 0) {
      await db.insert(storeLoyaltyPoints).values({
        storeId: order.storeId,
        customerPhone: order.customerPhone,
        points: pointsToEarn,
        type: 'earned',
        orderId: order.id,
        notes: `كسب ${pointsToEarn} نقطة من طلب #${order.orderNumber}`,
      })
    }
  }
}
```

### 4.7 استبدال النقاط في Checkout

**الملف المتأثر**: `src/app/api/checkout/route.ts` (أو مسار الـ checkout)

```typescript
// عند إنشاء الطلب، لو العميل أرسل loyaltyPointsToRedeem:

if (loyaltyPointsToRedeem && loyaltyPointsToRedeem > 0) {
  const settings = store.settings as LoyaltySettings
  if (!settings.loyaltyEnabled) {
    return ApiErrors.validation('نظام الولاء غير مفعل')
  }

  // تحقق من الرصيد
  const [balanceResult] = await db
    .select({ balance: sql<number>`COALESCE(SUM(points), 0)::int` })
    .from(storeLoyaltyPoints)
    .where(and(
      eq(storeLoyaltyPoints.storeId, store.id),
      eq(storeLoyaltyPoints.customerPhone, customerPhone),
    ))

  const balance = balanceResult?.balance ?? 0

  if (loyaltyPointsToRedeem > balance) {
    return ApiErrors.validation('رصيد النقاط غير كافي')
  }

  if (loyaltyPointsToRedeem < settings.loyaltyMinRedemption) {
    return ApiErrors.validation(`أقل عدد نقاط للاستبدال هو ${settings.loyaltyMinRedemption}`)
  }

  const discount = loyaltyPointsToRedeem * settings.loyaltyPointValue
  const maxDiscount = subtotal * (settings.loyaltyMaxRedemptionPercent / 100)
  const actualDiscount = Math.min(discount, maxDiscount)

  // خصم النقاط
  await db.insert(storeLoyaltyPoints).values({
    storeId: store.id,
    customerPhone,
    points: -loyaltyPointsToRedeem,
    type: 'redeemed',
    orderId: newOrder.id,
    notes: `استبدال ${loyaltyPointsToRedeem} نقطة في طلب #${orderNumber}`,
  })

  // طبّق الخصم على الطلب
  totalAmount -= actualDiscount
}
```

### 4.8 واجهة Checkout — عرض النقاط

في صفحة Checkout بالمتجر (Storefront):

```
لو loyaltyEnabled && العميل أدخل رقم هاتفه:
├── أظهر: "🎁 لديك X نقطة (تساوي Y جنيه)"
├── Input: عدد النقاط المراد استبدالها
│   └── Validation: >= minRedemption, <= balance
├── أظهر: "خصم بالنقاط: -Y EGP"
└── لو النقاط أقل من الحد الأدنى:
    └── أظهر: "تحتاج X نقاط أخرى للاستبدال"
```

---

## 5. نظام التسويق بالعمولة (Affiliate System)

### 5.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | التاجر ينشئ برنامج أفلييت — المسوقين يشاركوا رابط إحالة ويكسبوا عمولة |
| **ليه مهم** | يخلي أي حد يسوّق للتاجر مقابل عمولة |
| **الجهد** | عالي (middleware + checkout + لوحة تحكم) |

### 5.2 بنية الملفات

```
src/app/
├── (dashboard)/dashboard/affiliates/
│   ├── page.tsx                       # قائمة المسوقين
│   ├── new/
│   │   └── page.tsx                   # إضافة مسوق
│   └── [id]/
│       └── page.tsx                   # تفاصيل المسوق + مبيعاته
│
├── api/dashboard/affiliates/
│   ├── route.ts                       # GET (list), POST (create)
│   └── [id]/
│       ├── route.ts                   # GET, PUT, DELETE
│       └── sales/
│           └── route.ts              # GET (sales for affiliate)
│
└── store/affiliate/
    └── page.tsx                       # صفحة تسجيل المسوقين (عامة)
```

### 5.3 API — إدارة المسوقين

**ملف**: `src/app/api/dashboard/affiliates/route.ts`

```typescript
import { z } from 'zod'

const affiliateSchema = z.object({
  name: z.string().min(2).max(100),
  email: z.string().email().optional().nullable(),
  phone: z.string().min(10).max(15),
  code: z.string().min(3).max(30).regex(/^[A-Za-z0-9_-]+$/),
  commissionRate: z.coerce.number().min(0.01).max(50),
  isActive: z.boolean().default(true),
})

// POST — إنشاء مسوق
export async function POST(request: NextRequest) {
  const { storeId } = await verifyStoreOwnership(request)
  const body = await request.json()
  const data = affiliateSchema.parse(body)

  const [affiliate] = await db
    .insert(storeAffiliates)
    .values({ storeId, ...data })
    .returning()

  return apiSuccess(affiliate, 201)
}

// GET — قائمة المسوقين + إحصائياتهم
export async function GET(request: NextRequest) {
  const { storeId } = await verifyStoreOwnership(request)

  const affiliates = await db
    .select()
    .from(storeAffiliates)
    .where(eq(storeAffiliates.storeId, storeId))
    .orderBy(desc(storeAffiliates.createdAt))

  return apiSuccess(affiliates)
}
```

### 5.4 تتبع الإحالة (Referral Tracking)

**المنطق**: عندما يزور العميل الرابط `ahmed.matjary.com/?ref=AFFILIATE_CODE`:

**الملف المتأثر**: `src/middleware.ts`

```typescript
// في بداية الـ middleware، قبل أي routing:

// P3: Affiliate tracking
const refCode = request.nextUrl.searchParams.get('ref')
if (refCode) {
  const response = NextResponse.next()
  response.cookies.set('matjary_ref', refCode, {
    path: '/',
    sameSite: 'lax',
    secure: process.env.NODE_ENV === 'production',
    maxAge: 60 * 60 * 24 * 30, // 30 يوم
    httpOnly: true,
  })
  // أكمل باقي الـ middleware عادي
}
```

**عند إنشاء الطلب في Checkout**:

```typescript
// الملف المتأثر: src/app/api/checkout/route.ts أو مسار إنشاء الطلب

const refCode = request.cookies.get('matjary_ref')?.value

if (refCode) {
  // ابحث عن المسوق
  const [affiliate] = await db
    .select()
    .from(storeAffiliates)
    .where(and(
      eq(storeAffiliates.storeId, storeId),
      eq(storeAffiliates.code, refCode),
      eq(storeAffiliates.isActive, true),
    ))
    .limit(1)

  if (affiliate) {
    const commissionAmount = Number(orderTotal) * (Number(affiliate.commissionRate) / 100)

    // سجل البيعة
    await db.insert(storeAffiliateSales).values({
      storeId,
      affiliateId: affiliate.id,
      orderId: newOrder.id,
      saleAmount: orderTotal.toString(),
      commissionAmount: commissionAmount.toFixed(2),
      status: 'pending',
    })

    // حدّث إحصائيات المسوق
    await db
      .update(storeAffiliates)
      .set({
        totalSales: sql`${storeAffiliates.totalSales} + ${orderTotal}`,
        totalCommission: sql`${storeAffiliates.totalCommission} + ${commissionAmount.toFixed(2)}`,
        pendingCommission: sql`${storeAffiliates.pendingCommission} + ${commissionAmount.toFixed(2)}`,
      })
      .where(eq(storeAffiliates.id, affiliate.id))
  }
}
```

### 5.5 صفحة الأفلييت العامة

**ملف**: `src/app/store/affiliate/page.tsx`

```
صفحة بسيطة:
├── عنوان: "كسب عمولات مع ${storeName}"
├── وصف البرنامج
├── فورم تسجيل:
│   ├── الاسم
│   ├── الهاتف
│   └── الإيميل (اختياري)
└── بعد التسجيل: يظهر الكود + الرابط مع زر نسخ
```

**API تسجيل مسوق جديد**: `POST /api/storefront/affiliate/register`

```typescript
// يقبل: { name, phone, email? }
// يولّد كود فريد (nanoid أو slug من الاسم)
// يرجع: { code, link }
// الكود: `${name_slug}_${random4digits}`
```

### 5.6 لوحة تحكم المسوق (صفحة عامة)

**ملف**: `src/app/store/affiliate/dashboard/page.tsx`

```
يتحقق من الكود عبر query param: ?code=XXX
├── إحصائيات:
│   ├── عدد النقرات (اختياري — يحتاج tracking منفصل)
│   ├── عدد المبيعات
│   ├── إجمالي الأرباح
│   └── الأرباح المعلقة
├── جدول المبيعات الأخيرة
└── زر "نسخ رابط الإحالة"
```

### 5.7 موافقة التاجر على العمولات

في صفحة تفاصيل المسوق في Dashboard:

```
├── قائمة المبيعات: اسم الطلب، المبلغ، العمولة، الحالة
├── زر "اعتماد" → يغير status من 'pending' لـ 'approved'
├── زر "دفع" → يغير status من 'approved' لـ 'paid'
│   └── يطرح المبلغ من pendingCommission
└── زر "رفض" → يغير status لـ 'cancelled'
```

---

## 6. نظام Dropshipping المدمج

### 6.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | التاجر يربط منتجاته بموردين — يبيع بدون مخزون |
| **ليه مهم** | سوق ضخم في مصر — ناس كتير عايزة تبيع بدون مخزون |
| **الجهد** | عالي |

### 6.2 بنية الملفات

```
src/app/
├── (dashboard)/dashboard/dropshipping/
│   ├── page.tsx                       # قائمة المنتجات المربوطة بموردين
│   └── link/
│       └── page.tsx                   # ربط منتج بمورد
│
├── api/dashboard/dropshipping/
│   ├── route.ts                       # GET (list), POST (create link)
│   └── [id]/
│       └── route.ts                   # PUT, DELETE
```

### 6.3 API — ربط المنتجات بالموردين

**ملف**: `src/app/api/dashboard/dropshipping/route.ts`

```typescript
const supplierProductSchema = z.object({
  productId: z.string().uuid().optional().nullable(),
  supplierName: z.string().min(1).max(200),
  supplierProductUrl: z.string().url().optional().nullable(),
  supplierPrice: z.coerce.number().min(0),
  retailPrice: z.coerce.number().min(0),
  autoOrder: z.boolean().default(false),
  leadTimeDays: z.coerce.number().int().min(1).max(365).default(7),
  notes: z.string().max(500).optional().nullable(),
})

// POST — ربط منتج بمورد
export async function POST(request: NextRequest) {
  const { storeId } = await verifyStoreOwnership(request)
  const body = await request.json()
  const data = supplierProductSchema.parse(body)

  // تحقق: هامش الربح موجب
  if (data.retailPrice <= data.supplierPrice) {
    return ApiErrors.validation('سعر البيع لازم يكون أكبر من سعر المورد')
  }

  const [link] = await db
    .insert(storeSupplierProducts)
    .values({ storeId, ...data })
    .returning()

  return apiSuccess(link, 201)
}

// GET — كل الروابط
export async function GET(request: NextRequest) {
  const { storeId } = await verifyStoreOwnership(request)

  const links = await db
    .select({
      supplierProduct: storeSupplierProducts,
      productName: storeProducts.name,
      productImage: storeProducts.images,
    })
    .from(storeSupplierProducts)
    .leftJoin(storeProducts, eq(storeSupplierProducts.productId, storeProducts.id))
    .where(eq(storeSupplierProducts.storeId, storeId))
    .orderBy(desc(storeSupplierProducts.createdAt))

  return apiSuccess(links)
}
```

### 6.4 صفحة Dashboard — قائمة Dropshipping

```
الصفحة تحتوي:
├── إحصائيات:
│   ├── عدد المنتجات المربوطة
│   ├── متوسط هامش الربح
│   └── إجمالي الأرباح المتوقعة
├── جدول:
│   ├── المنتج (اسم + صورة)
│   ├── المورد
│   ├── سعر المورد
│   ├── سعر البيع
│   ├── هامش الربح
│   └── إجراءات (تعديل، حذف)
└── زر "ربط منتج جديد"
```

### 6.5 فورم ربط المنتج بالمورد

```
الفورم يحتوي:
├── اختيار المنتج (dropdown من منتجات المتجر)
├── اسم المورد
├── رابط المنتج عند المورد (اختياري)
├── سعر المورد
├── سعر البيع (يظهر هامش الربح تلقائياً)
├── مدة التوصيل المتوقعة (أيام)
├── Toggle: طلب تلقائي للمورد
└── ملاحظات
```

### 6.6 إشعار عند طلب جديد

عند وصول طلب على منتج dropshipping:

```typescript
// الملف المتأثر: عند إنشاء الطلب (checkout flow)

// بعد إنشاء الطلب:
// ابحث هل أي منتج في الطلب مربوط بمورد
const supplierLinks = await db
  .select()
  .from(storeSupplierProducts)
  .where(and(
    eq(storeSupplierProducts.storeId, storeId),
    inArray(storeSupplierProducts.productId, orderProductIds),
  ))

if (supplierLinks.length > 0) {
  // سجل في Activity Log
  await db.insert(platformActivityLog).values({
    storeId,
    merchantId: store.merchantId,
    action: 'dropship_order_received',
    entity: 'order',
    entityId: newOrder.id,
    metadata: {
      suppliers: supplierLinks.map(s => ({
        supplierName: s.supplierName,
        productId: s.productId,
        supplierUrl: s.supplierProductUrl,
      })),
    },
  })

  // اختياري: أرسل WhatsApp أو إشعار للتاجر
}
```

---

## 7. تكامل شركات الشحن المصرية

### 7.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | ربط المتجر مع شركات الشحن (Bosta, Aramex, J&T, Mylerz) لإنشاء بوليصة + تتبع تلقائي |
| **ليه مهم** | التاجر يوفر وقت ويقلل أخطاء — كل حاجة من داخل المنصة |
| **الجهد** | عالي (APIs خارجية مختلفة) |

### 7.2 بنية الملفات

```
src/lib/
└── shipping/
    ├── types.ts                    # أنواع مشتركة
    ├── provider-factory.ts         # Factory pattern
    ├── providers/
    │   ├── bosta.ts               # Bosta API integration
    │   ├── aramex.ts              # Aramex API integration
    │   ├── jnt.ts                 # J&T API integration
    │   └── mylerz.ts             # Mylerz API integration

src/app/
├── (dashboard)/dashboard/settings/shipping-accounts/
│   └── page.tsx                   # إعداد حسابات شركات الشحن
│
├── api/dashboard/shipping-accounts/
│   ├── route.ts                   # GET (list), POST (create)
│   └── [id]/
│       └── route.ts              # PUT, DELETE
│
├── api/dashboard/orders/[id]/
│   └── create-shipment/
│       └── route.ts              # POST — إنشاء بوليصة شحن
```

### 7.3 Provider Types

**ملف**: `src/lib/shipping/types.ts`

```typescript
export type ShippingProvider = 'bosta' | 'aramex' | 'jnt' | 'mylerz'

export type CreateShipmentInput = {
  orderId: string
  orderNumber: string
  customerName: string
  customerPhone: string
  address: {
    governorate: string
    city: string
    area: string
    street: string
    building?: string
    floor?: string
    apartment?: string
  }
  codAmount: number        // مبلغ الدفع عند الاستلام
  weight: number           // بالكيلو
  description: string      // وصف المحتويات
  pieces: number           // عدد القطع
}

export type ShipmentResult = {
  trackingNumber: string
  waybillUrl?: string      // رابط بوليصة PDF
  estimatedDelivery?: string
  providerOrderId: string
}

export type TrackingUpdate = {
  status: string
  description: string
  timestamp: string
  location?: string
}

export interface ShippingProviderClient {
  createShipment(input: CreateShipmentInput): Promise<ShipmentResult>
  trackShipment(trackingNumber: string): Promise<TrackingUpdate[]>
  cancelShipment(trackingNumber: string): Promise<boolean>
}
```

### 7.4 Provider Factory

**ملف**: `src/lib/shipping/provider-factory.ts`

```typescript
import type { ShippingProvider, ShippingProviderClient } from './types'
import { BostaProvider } from './providers/bosta'
import { AramexProvider } from './providers/aramex'
import { JntProvider } from './providers/jnt'
import { MylerzProvider } from './providers/mylerz'

export function createShippingProvider(
  provider: ShippingProvider,
  credentials: { apiKey: string; apiSecret?: string; accountId?: string; settings?: Record<string, unknown> },
): ShippingProviderClient {
  switch (provider) {
    case 'bosta':
      return new BostaProvider(credentials)
    case 'aramex':
      return new AramexProvider(credentials)
    case 'jnt':
      return new JntProvider(credentials)
    case 'mylerz':
      return new MylerzProvider(credentials)
    default:
      throw new Error(`Unsupported shipping provider: ${provider}`)
  }
}
```

### 7.5 مثال: Bosta Provider

**ملف**: `src/lib/shipping/providers/bosta.ts`

```typescript
import type { ShippingProviderClient, CreateShipmentInput, ShipmentResult, TrackingUpdate } from '../types'

const BOSTA_API_URL = 'https://app.bosta.co/api/v2'

export class BostaProvider implements ShippingProviderClient {
  private apiKey: string

  constructor(credentials: { apiKey: string }) {
    this.apiKey = credentials.apiKey
  }

  async createShipment(input: CreateShipmentInput): Promise<ShipmentResult> {
    const response = await fetch(`${BOSTA_API_URL}/deliveries`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': this.apiKey,
      },
      body: JSON.stringify({
        type: 10, // delivery
        specs: {
          packageType: 'Parcel',
          size: 'SMALL',
          packageDetails: {
            itemsCount: input.pieces,
            description: input.description,
          },
        },
        dropOffAddress: {
          firstLine: `${input.address.street} ${input.address.building || ''}`.trim(),
          city: input.address.city,
          zone: input.address.area,
          district: input.address.governorate,
        },
        receiver: {
          firstName: input.customerName.split(' ')[0] || input.customerName,
          lastName: input.customerName.split(' ').slice(1).join(' ') || '',
          phone: input.customerPhone,
        },
        cod: input.codAmount,
        businessReference: input.orderNumber,
      }),
    })

    if (!response.ok) {
      const error = await response.json().catch(() => ({}))
      throw new Error(`Bosta API error: ${error.message || response.statusText}`)
    }

    const data = await response.json()

    return {
      trackingNumber: data.trackingNumber,
      waybillUrl: data.awbUrl,
      providerOrderId: data._id,
    }
  }

  async trackShipment(trackingNumber: string): Promise<TrackingUpdate[]> {
    const response = await fetch(`${BOSTA_API_URL}/deliveries/tracking/${trackingNumber}`, {
      headers: { 'Authorization': this.apiKey },
    })

    if (!response.ok) throw new Error('Failed to track shipment')

    const data = await response.json()

    return (data.TransitEvents || []).map((event: Record<string, string>) => ({
      status: event.state,
      description: event.msg || event.state,
      timestamp: event.timestamp,
    }))
  }

  async cancelShipment(trackingNumber: string): Promise<boolean> {
    const response = await fetch(`${BOSTA_API_URL}/deliveries/${trackingNumber}`, {
      method: 'DELETE',
      headers: { 'Authorization': this.apiKey },
    })
    return response.ok
  }
}
```

> **ملاحظة**: Aramex, J&T, Mylerz — نفس الـ interface ولكن بـ API مختلف.  
> كل provider يحتاج يطلع على الـ API Documentation الخاص بيه.  
> ابدأ بـ **Bosta** (الأكثر استخداماً في مصر) ثم أضف الباقي.

### 7.6 API — إنشاء بوليصة شحن

**ملف**: `src/app/api/dashboard/orders/[id]/create-shipment/route.ts`

```typescript
import { createShippingProvider } from '@/lib/shipping/provider-factory'

export async function POST(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
) {
  try {
    const { storeId } = await verifyStoreOwnership(request)
    const { id: orderId } = await params
    const body = await request.json()
    const { provider, weight = 1, pieces = 1 } = body

    // 1. جلب الطلب
    const [order] = await db
      .select()
      .from(storeOrders)
      .where(and(
        eq(storeOrders.id, orderId),
        eq(storeOrders.storeId, storeId),
      ))
      .limit(1)

    if (!order) return ApiErrors.notFound('الطلب')

    // 2. جلب بيانات شركة الشحن
    const [account] = await db
      .select()
      .from(storeShippingAccounts)
      .where(and(
        eq(storeShippingAccounts.storeId, storeId),
        eq(storeShippingAccounts.provider, provider),
        eq(storeShippingAccounts.isActive, true),
      ))
      .limit(1)

    if (!account) return ApiErrors.validation('لم يتم إعداد شركة الشحن هذه')

    // 3. إنشاء البوليصة
    const shippingClient = createShippingProvider(account.provider as ShippingProvider, {
      apiKey: account.apiKey,
      apiSecret: account.apiSecret ?? undefined,
      accountId: account.accountId ?? undefined,
      settings: account.settings as Record<string, unknown>,
    })

    const address = order.shippingAddress as ShippingAddress

    const result = await shippingClient.createShipment({
      orderId: order.id,
      orderNumber: order.orderNumber ?? orderId.slice(0, 8),
      customerName: order.customerName,
      customerPhone: order.customerPhone,
      address,
      codAmount: order.paymentMethod === 'cod' ? Number(order.totalAmount) : 0,
      weight,
      description: `طلب #${order.orderNumber}`,
      pieces,
    })

    // 4. حدّث الطلب
    await db
      .update(storeOrders)
      .set({
        trackingNumber: result.trackingNumber,
        shippingCompany: provider,
        orderStatus: 'processing',
        updatedAt: new Date(),
      })
      .where(eq(storeOrders.id, orderId))

    return apiSuccess(result)
  } catch (error) {
    return handleApiError(error)
  }
}
```

### 7.7 صفحة إعداد حسابات الشحن

**ملف**: `src/app/(dashboard)/dashboard/settings/shipping-accounts/page.tsx`

```
الصفحة:
├── قائمة الشركات المدعومة:
│   ├── Bosta — [متصل ✅] أو [إعداد]
│   ├── Aramex — [متصل ✅] أو [إعداد]
│   ├── J&T — [متصل ✅] أو [إعداد]
│   └── Mylerz — [متصل ✅] أو [إعداد]
│
├── عند الضغط على "إعداد":
│   ├── Dialog/Sheet:
│   │   ├── API Key (مطلوب)
│   │   ├── API Secret (اختياري حسب الشركة)
│   │   ├── Account ID (اختياري)
│   │   └── زر "حفظ واختبار الاتصال"
│   └── يختبر الاتصال برسالة test قبل الحفظ
│
└── رابط من صفحة الإعدادات الرئيسية
```

### 7.8 زر إنشاء بوليصة في صفحة الطلب

في صفحة تفاصيل الطلب بالـ Dashboard:

```
لو الطلب حالته confirmed أو processing ومعندوش trackingNumber:
├── زر "📦 إنشاء بوليصة شحن"
└── Dialog:
    ├── اختيار شركة الشحن (من الشركات المتصلة)
    ├── الوزن (kg)
    ├── عدد القطع
    └── زر "إنشاء"

بعد الإنشاء:
├── يظهر رقم التتبع
├── رابط بوليصة PDF (لو متاح)
└── حالة الطلب تتحدث لـ processing
```

---

## 8. Multi-language Support (دعم لغات متعددة)

### 8.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | المتجر يدعم عرض المنتجات بأكثر من لغة (عربي/إنجليزي) |
| **ليه مهم** | للتجار اللي بيبيعوا لعملاء أجانب (سياحة، تصدير) |
| **الجهد** | متوسط |

### 8.2 النهج المختار

**النهج**: Product-level translations في عمود `translations` (JSONB) + URL query param `?lang=en`

**ليه مش `next-intl`**: المنصة RTL-first وكل الواجهات ثابتة بالعربي. الترجمة فقط على **محتوى المنتجات** مش الـ UI.

### 8.3 بنية الترجمات

```typescript
// النوع المُضاف في storeProducts.translations:
type ProductTranslations = {
  en?: {
    name?: string
    description?: string
    shortDescription?: string
  }
  fr?: {
    name?: string
    description?: string
    shortDescription?: string
  }
  // يمكن إضافة أي لغة
}
```

### 8.4 إعدادات اللغة (في store.settings JSONB)

```typescript
type LanguageSettings = {
  defaultLanguage: 'ar'              // دايماً عربي كافتراضي
  supportedLanguages: string[]        // مثلاً ['ar', 'en']
}
```

### 8.5 تعديل واجهة المنتج في Dashboard

في صفحة إنشاء/تعديل المنتج:

```
لو supportedLanguages.length > 1:
├── Tabs: العربية | English | ...
├── كل tab يحتوي:
│   ├── اسم المنتج بالـ [language]
│   ├── الوصف بالـ [language]
│   └── الوصف القصير بالـ [language]
└── الـ Arabic tab = الحقول الأصلية (name, description, shortDescription)
    الترجمات تحُفظ في translations JSONB
```

### 8.6 عرض الترجمة في Storefront

**Helper function**: `src/lib/products/translate.ts`

```typescript
type TranslatableProduct = {
  name: string
  description: string | null
  shortDescription: string | null
  translations: Record<string, { name?: string; description?: string; shortDescription?: string }> | null
}

export function translateProduct<T extends TranslatableProduct>(
  product: T,
  lang: string,
): T {
  if (lang === 'ar' || !product.translations) return product
  
  const translation = product.translations[lang]
  if (!translation) return product

  return {
    ...product,
    name: translation.name || product.name,
    description: translation.description || product.description,
    shortDescription: translation.shortDescription || product.shortDescription,
  }
}
```

**استخدامها في API**:

```typescript
// في /api/storefront/ endpoints:
const lang = request.nextUrl.searchParams.get('lang') || 'ar'

// بعد جلب المنتجات:
const translatedProducts = products.map(p => translateProduct(p, lang))
```

### 8.7 Language Switcher Component

**ملف**: `src/app/store/_components/language-switcher.tsx`

```typescript
'use client'

import { useRouter, useSearchParams } from 'next/navigation'

const LANGUAGES = [
  { code: 'ar', label: 'العربية', dir: 'rtl' },
  { code: 'en', label: 'English', dir: 'ltr' },
] as const

export function LanguageSwitcher({ supportedLanguages }: { supportedLanguages: string[] }) {
  const router = useRouter()
  const searchParams = useSearchParams()
  const currentLang = searchParams.get('lang') || 'ar'

  const availableLanguages = LANGUAGES.filter(l => supportedLanguages.includes(l.code))

  if (availableLanguages.length <= 1) return null

  const handleChange = (lang: string) => {
    const params = new URLSearchParams(searchParams.toString())
    if (lang === 'ar') {
      params.delete('lang')
    } else {
      params.set('lang', lang)
    }
    const query = params.toString()
    router.push(`?${query}`)
  }

  return (
    <div className="flex items-center gap-1">
      {availableLanguages.map((lang) => (
        <button
          key={lang.code}
          onClick={() => handleChange(lang.code)}
          className={`px-2 py-1 text-xs rounded ${
            currentLang === lang.code
              ? 'bg-primary text-primary-foreground'
              : 'text-muted-foreground hover:text-foreground'
          }`}
        >
          {lang.label}
        </button>
      ))}
    </div>
  )
}
```

### 8.8 تعديل Store Layout

**الملف**: `src/app/store/layout.tsx`

```typescript
// أضف LanguageSwitcher في الـ header
// اقرأ lang من searchParams
// غيّر dir="rtl" لـ dir="ltr" لو lang=en

const lang = searchParams.get('lang') || 'ar'
const dir = lang === 'ar' ? 'rtl' : 'ltr'

// في الـ HTML:
<div dir={dir}>
  {children}
</div>
```

---

## 9. تطبيق موبايل أصلي (Mobile App Planning)

### 9.1 نظرة عامة

| البند | التفاصيل |
|-------|----------|
| **ماهو** | تطبيق موبايل للتاجر (إدارة) + تطبيق للعملاء (اختياري) |
| **النهج** | React Native (أو Expo) — لأن الفريق يعرف React |
| **الحالة** | تخطيط فقط — التنفيذ في مشروع منفصل |

### 9.2 لماذا React Native؟

- **نفس لغة المنصة**: TypeScript + React
- **Expo**: يسهل البناء والنشر
- **Code sharing**: مكتبات Zod validations + Types مشتركة
- **Push Notifications**: Expo Push أو Firebase

### 9.3 API Requirements

كل الـ APIs الموجودة حالياً جاهزة لتطبيق الموبايل:
- Dashboard APIs (`/api/dashboard/*`) — تحتاج Clerk token
- Storefront APIs (`/api/storefront/*`) — عامة
- Authentication — Clerk React Native SDK

**APIs إضافية مطلوبة**:

| API | الوصف |
|-----|-------|
| `POST /api/dashboard/push-token` | تسجيل FCM/Expo push token |
| `GET /api/dashboard/notifications` | قائمة الإشعارات |
| `PATCH /api/dashboard/notifications/[id]/read` | تعليم إشعار كمقروء |

### 9.4 Push Notifications

**أنواع الإشعارات**:

| الحدث | المستقبل | الرسالة |
|-------|---------|---------|
| طلب جديد | التاجر | "طلب جديد #123 بقيمة 500 EGP" |
| تحديث حالة الطلب | العميل | "طلبك #123 في الطريق" |
| عربة مهجورة | العميل | "نسيت حاجة في السلة!" |
| بيعة أفلييت | المسوق | "مبروك! حققت بيعة جديدة" |

### 9.5 بنية مشروع الموبايل المقترحة

```
matjary-mobile/
├── app/                      # Expo Router screens
│   ├── (merchant)/           # شاشات التاجر
│   │   ├── dashboard.tsx
│   │   ├── orders/
│   │   ├── products/
│   │   └── settings/
│   └── (store)/              # شاشات العميل (اختياري)
│       ├── home.tsx
│       ├── products/
│       ├── cart.tsx
│       └── checkout.tsx
├── lib/
│   ├── api-client.ts         # HTTP client مع Clerk token
│   ├── types.ts              # أنواع مشتركة (من المنصة)
│   └── push.ts               # Push notification setup
└── package.json
```

### 9.6 خطوات التنفيذ المستقبلية

1. إنشاء مشروع Expo: `npx create-expo-app matjary-mobile`
2. إعداد Clerk React Native
3. بناء شاشات: Dashboard → Orders → Products
4. إعداد Push Notifications
5. نشر على App Store + Google Play

> **هذه الميزة للتخطيط فقط** — التنفيذ في مشروع منفصل بعد اكتمال P3 الباقي.

---

## ملخص الملفات الجديدة

| الملف | الوصف |
|-------|-------|
| `migrations/p3_advanced_features.sql` | كل جداول P3 + التعديلات |
| `src/lib/coupons/auto-apply.ts` | منطق الكوبون التلقائي |
| `src/lib/shipping/types.ts` | أنواع شركات الشحن |
| `src/lib/shipping/provider-factory.ts` | Factory pattern |
| `src/lib/shipping/providers/bosta.ts` | Bosta API |
| `src/lib/shipping/providers/aramex.ts` | Aramex API |
| `src/lib/shipping/providers/jnt.ts` | J&T API |
| `src/lib/shipping/providers/mylerz.ts` | Mylerz API |
| `src/lib/products/translate.ts` | ترجمة المنتجات helper |
| `src/app/(dashboard)/dashboard/blog/` | صفحات المدونة Dashboard |
| `src/app/(dashboard)/dashboard/affiliates/` | صفحات الأفلييت Dashboard |
| `src/app/(dashboard)/dashboard/loyalty/` | صفحة الولاء Dashboard |
| `src/app/(dashboard)/dashboard/dropshipping/` | صفحات Dropshipping Dashboard |
| `src/app/(dashboard)/dashboard/settings/shipping-accounts/` | إعداد شركات الشحن |
| `src/app/api/dashboard/blog/` | API المدونة |
| `src/app/api/dashboard/affiliates/` | API الأفلييت |
| `src/app/api/dashboard/loyalty/` | API الولاء |
| `src/app/api/dashboard/dropshipping/` | API Dropshipping |
| `src/app/api/dashboard/shipping-accounts/` | API حسابات الشحن |
| `src/app/api/dashboard/orders/[id]/create-shipment/` | إنشاء بوليصة |
| `src/app/api/storefront/blog/` | API المدونة العامة |
| `src/app/api/storefront/loyalty/balance/` | رصيد النقاط |
| `src/app/api/storefront/affiliate/register/` | تسجيل مسوق |
| `src/app/store/blog/` | صفحات المدونة Storefront |
| `src/app/store/affiliate/` | صفحات الأفلييت العامة |
| `src/app/store/offline/` | صفحة أوفلاين |
| `src/app/store/manifest.json/` | Dynamic manifest |
| `src/app/store/_components/pwa-install-banner.tsx` | بانر تثبيت PWA |
| `src/app/store/_components/language-switcher.tsx` | تبديل اللغة |
| `public/sw.js` | Service Worker |
| `public/icons/icon-192.png` | أيقونة PWA |
| `public/icons/icon-512.png` | أيقونة PWA |

## ملخص الملفات المعدَّلة

| الملف | التعديل |
|-------|---------|
| `src/db/schema.ts` | إضافة 6 جداول جديدة + تعديل storeCoupons + storeProducts |
| `src/middleware.ts` | إضافة affiliate ref cookie tracking |
| `src/app/(dashboard)/dashboard-layout-shell.tsx` | إضافة أيقونات sidebar جديدة |
| `src/app/(dashboard)/layout.tsx` | إضافة items الـ sidebar الجديدة |
| `src/app/store/layout.tsx` | PWA manifest + SW + language dir |
| `src/app/store/_components/store-header.tsx` | رابط المدونة + language switcher |
| `src/lib/validations/order.ts` | حقول الكوبون الجديدة |
| `src/app/api/coupons/validate/route.ts` | تحققات الكوبون المتقدمة |
| `src/app/api/dashboard/orders/[id]/status/route.ts` | منح نقاط ولاء عند delivered |
| `src/app/api/checkout/route.ts` | استبدال نقاط + affiliate tracking |

---

> **نهاية دليل P3** — آخر مرحلة في خطة المميزات الـ 27.  
> بعد إنتهاء P3، المنصة تكون فيها كل المميزات المخططة 🚀
